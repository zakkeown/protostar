---
phase: 10.1-boundary-hygiene-pass
plan: 06
subsystem: boundary-hygiene
tags: [factory-cli, publish]
key-files:
  created: [tools/factory-cli-pack-smoke.sh]
  modified: [apps/factory-cli/package.json, apps/factory-cli/tsconfig.json, .gitignore]
metrics:
  pack_smoke_scripts_added: 1
---

# Plan 10.1.6 Summary — Factory CLI Publish Wiring

## What Changed

Confirmed `@protostar/factory-cli` is publish-ready with `private: false`, public publish config, Node 22 engines, and preserved subpath exports. Added `tools/factory-cli-pack-smoke.sh` to build, pack, install into an isolated temp prefix, and verify `protostar-factory --help`. Added a tarball ignore rule and aligned the factory-cli tsconfig with its `@protostar/dogpile-types` dependency.

## Verification

- `pnpm run verify` — PASS

## Deviations

The pack smoke script was added but not run during this execution to avoid extra npm install churn outside the required verify gate.

## Self-Check: PASSED
