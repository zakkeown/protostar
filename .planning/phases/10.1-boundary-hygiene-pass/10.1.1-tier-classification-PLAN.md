---
phase: 10.1-boundary-hygiene-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admission-e2e/package.json
  - packages/artifacts/package.json
  - packages/authority/package.json
  - packages/delivery/package.json
  - packages/delivery-runtime/package.json
  - packages/dogpile-adapter/package.json
  - packages/dogpile-types/package.json
  - packages/evaluation/package.json
  - packages/evaluation-runner/package.json
  - packages/execution/package.json
  - packages/fixtures/package.json
  - packages/intent/package.json
  - packages/lmstudio-adapter/package.json
  - packages/mechanical-checks/package.json
  - packages/paths/package.json
  - packages/planning/package.json
  - packages/policy/package.json
  - packages/repair/package.json
  - packages/repo/package.json
  - packages/review/package.json
  - apps/factory-cli/package.json
  - AGENTS.md
autonomous: true
requirements: [BOUNDARY-01, BOUNDARY-02]

must_haves:
  truths:
    - "Every packages/* and apps/factory-cli package.json declares protostar.tier"
    - "AGENTS.md Authority Tiers table lists every package with its tier"
    - "AGENTS.md documents the protostar.tier field schema"
    - "@protostar/paths remains classified fs (carve-out preserved)"
    - "@protostar/admission-e2e is classified test-only"
  artifacts:
    - path: "AGENTS.md"
      provides: "Authority Tiers table + tier field schema documentation"
      contains: "protostar.tier"
  key_links:
    - from: "packages/*/package.json"
      to: "AGENTS.md tier table"
      via: "protostar.tier field declaration"
      pattern: "\"protostar\":\\s*\\{\\s*\"tier\""
---

<objective>
Add the `protostar.tier` field to every package manifest and rewrite AGENTS.md's Authority Tiers table so package manifests become the load-bearing source of truth for tier classification (per CONTEXT.md D-Q07). Subsequent plans (Plan 4 conformance gate, Plan 5 hygiene sweep) read this field. No source code changes; manifest + docs only.

Purpose: BOUNDARY-01 + BOUNDARY-02. Establishes the contract every other 10.1 plan asserts against.
Output: 21 modified package.json files + AGENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-boundary-hygiene-pass/10.1-CONTEXT.md
@.planning/phases/10.1-boundary-hygiene-pass/10.1-RESEARCH.md
@AGENTS.md

<interfaces>
<!-- Tier field schema (load-bearing — Plan 4 conformance gate enforces). -->
Field shape (top-level in package.json):
```json
{
  "protostar": { "tier": "pure" | "fs" | "network" | "orchestration" | "test-only" }
}
```

Tier assignment (from RESEARCH.md inventory + CONTEXT.md Q-04..Q-07):

| Package | Tier |
|---------|------|
| apps/factory-cli | orchestration |
| packages/repo | fs |
| packages/paths | fs (carve-out — see AGENTS.md) |
| packages/dogpile-adapter | network |
| packages/delivery-runtime | network |
| packages/lmstudio-adapter | network |
| packages/evaluation-runner | network |
| packages/admission-e2e | test-only |
| packages/artifacts | pure |
| packages/authority | pure |
| packages/delivery | pure |
| packages/dogpile-types | pure |
| packages/evaluation | pure |
| packages/execution | pure |
| packages/fixtures | pure |
| packages/intent | pure |
| packages/mechanical-checks | pure |
| packages/planning | pure |
| packages/policy | pure |
| packages/repair | pure |
| packages/review | pure |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark pure-tier packages</name>
  <read_first>
    - packages/artifacts/package.json
    - packages/authority/package.json
    - .planning/phases/10.1-boundary-hygiene-pass/10.1-RESEARCH.md (inventory section)
  </read_first>
  <files>packages/artifacts/package.json, packages/authority/package.json, packages/delivery/package.json, packages/dogpile-types/package.json, packages/evaluation/package.json, packages/execution/package.json, packages/fixtures/package.json, packages/intent/package.json, packages/mechanical-checks/package.json, packages/planning/package.json, packages/policy/package.json, packages/repair/package.json, packages/review/package.json</files>
  <action>
    Add the top-level field `"protostar": { "tier": "pure" }` to each of the 13 pure-tier package.json files (per D-Q06). Place the field immediately after the `"name"` field for consistency. Do NOT touch any other field. Do NOT add `sideEffects` here (Plan 5 owns that). Do NOT remove any `@protostar/*` deps here (Plan 7 owns phantom-dep removal). Pure-tier means: zero filesystem authority, zero network authority. Per D-Q06 the four CONTEXT-flagged pure-tier additions (`mechanical-checks`, `repair`, `fixtures`, `admission-e2e` — note admission-e2e is in Task 5) all land here.
  </action>
  <verify>
    <automated>for p in artifacts authority delivery dogpile-types evaluation execution fixtures intent mechanical-checks planning policy repair review; do node -e "const m=require('./packages/$p/package.json'); if(m.protostar?.tier!=='pure'){console.error('FAIL',\"$p\",m.protostar);process.exit(1)}" || exit 1; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 13 listed package.json files contain `"protostar": { "tier": "pure" }` at top level.
    - No other fields modified (verifiable via `git diff --stat` showing only ~26 lines changed total — one block per file).
    - JSON remains valid (`node -e 'JSON.parse(require("fs").readFileSync("packages/<each>/package.json","utf8"))'` succeeds).
  </acceptance_criteria>
  <done>13 pure-tier manifests carry the tier field; no other manifest fields touched.</done>
</task>

<task type="auto">
  <name>Task 2: Mark fs-tier packages (carve-out preserved)</name>
  <read_first>
    - packages/repo/package.json
    - packages/paths/package.json
    - AGENTS.md (@protostar/paths Carve-Out section, lines 40-58)
  </read_first>
  <files>packages/repo/package.json, packages/paths/package.json</files>
  <action>
    Add `"protostar": { "tier": "fs" }` to both `packages/repo/package.json` and `packages/paths/package.json` (per D-Q06 carve-out preservation). Place after `"name"`. Do NOT change any other field. Do NOT promote `paths` to pure — its scope-ceiling carve-out is documented in AGENTS.md and remains in fs tier per CONTEXT specifics.
  </action>
  <verify>
    <automated>for p in repo paths; do node -e "const m=require('./packages/$p/package.json'); if(m.protostar?.tier!=='fs'){console.error('FAIL',\"$p\",m.protostar);process.exit(1)}" || exit 1; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/repo/package.json` and `packages/paths/package.json` contain `"protostar": { "tier": "fs" }`.
    - JSON valid.
    - No other field changes.
  </acceptance_criteria>
  <done>Both fs-tier manifests carry the tier field; paths carve-out preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Mark network-tier packages</name>
  <read_first>
    - packages/dogpile-adapter/package.json
    - packages/lmstudio-adapter/package.json
    - packages/delivery-runtime/package.json
    - packages/evaluation-runner/package.json
  </read_first>
  <files>packages/dogpile-adapter/package.json, packages/delivery-runtime/package.json, packages/lmstudio-adapter/package.json, packages/evaluation-runner/package.json</files>
  <action>
    Add `"protostar": { "tier": "network" }` to all four network-tier manifests (per D-Q04 + D-Q05 + existing AGENTS row). Place after `"name"`. Do NOT add the no-fs contract test here — Plan 2 owns that for `lmstudio-adapter`; the other three already have it (`dogpile-adapter`, `delivery-runtime`, `evaluation-runner`).
  </action>
  <verify>
    <automated>for p in dogpile-adapter delivery-runtime lmstudio-adapter evaluation-runner; do node -e "const m=require('./packages/$p/package.json'); if(m.protostar?.tier!=='network'){console.error('FAIL',\"$p\",m.protostar);process.exit(1)}" || exit 1; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 4 network-tier manifests contain `"protostar": { "tier": "network" }`.
    - JSON valid; no other field changes.
  </acceptance_criteria>
  <done>4 network-tier manifests carry the tier field.</done>
</task>

<task type="auto">
  <name>Task 4: Mark orchestration-tier (apps/factory-cli)</name>
  <read_first>
    - apps/factory-cli/package.json
  </read_first>
  <files>apps/factory-cli/package.json</files>
  <action>
    Add `"protostar": { "tier": "orchestration" }` to `apps/factory-cli/package.json` (the single orchestration-tier package — per AGENTS.md existing tier table). Place after `"name"`. Do NOT flip `private` or add `publishConfig` here — Plan 6 owns publish wiring.
  </action>
  <verify>
    <automated>node -e "const m=require('./apps/factory-cli/package.json'); if(m.protostar?.tier!=='orchestration'){console.error('FAIL',m.protostar);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/factory-cli/package.json` contains `"protostar": { "tier": "orchestration" }`.
    - JSON valid; no other field changes.
  </acceptance_criteria>
  <done>Orchestration-tier manifest carries tier field.</done>
</task>

<task type="auto">
  <name>Task 5: Mark test-only-tier (admission-e2e)</name>
  <read_first>
    - packages/admission-e2e/package.json
  </read_first>
  <files>packages/admission-e2e/package.json</files>
  <action>
    Add `"protostar": { "tier": "test-only" }` to `packages/admission-e2e/package.json` (per D-Q06 sub-tier). Place after `"name"`. The `test-only` tier is load-bearing: it sanctions `admission-e2e`'s "depends-on-everything" pattern that no other package may have. Plan 4's conformance gate enforces "test-only may depend on anything; nothing may depend on test-only."
  </action>
  <verify>
    <automated>node -e "const m=require('./packages/admission-e2e/package.json'); if(m.protostar?.tier!=='test-only'){console.error('FAIL',m.protostar);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/admission-e2e/package.json` contains `"protostar": { "tier": "test-only" }`.
    - JSON valid; no other field changes.
  </acceptance_criteria>
  <done>Test-only-tier manifest carries tier field.</done>
</task>

<task type="auto">
  <name>Task 6: Rewrite AGENTS.md Authority Tiers table + document tier field schema</name>
  <read_first>
    - AGENTS.md (full file — Authority Tiers section + paths Carve-Out section)
    - .planning/phases/10.1-boundary-hygiene-pass/10.1-CONTEXT.md (D-Q06, D-Q07, specifics on paths carve-out)
  </read_first>
  <files>AGENTS.md</files>
  <action>
    Replace the existing `## Authority Tiers` section (currently 4 bullet lines, lines 21-30 in current AGENTS.md) with a complete tier table covering every package. Use this exact structure:

    ```
    ## Authority Tiers

    Package tiers are declared in each `package.json` under the `protostar.tier` field. The workspace conformance gate (`packages/admission-e2e/src/tier-conformance.contract.test.ts`) reads these declarations as the source of truth.

    ### Tier Field Schema

    Every `packages/*/package.json` and `apps/factory-cli/package.json` MUST declare:

    ```json
    { "protostar": { "tier": "pure" | "fs" | "network" | "orchestration" | "test-only" } }
    ```

    ### Tier Table

    | Tier | Package | Authority | Notes |
    |------|---------|-----------|-------|
    | orchestration | `apps/factory-cli` | fs + network | only orchestration-tier package |
    | fs | `packages/repo` | fs (no network) | runtime fs-authority owner |
    | fs | `packages/paths` | fs (carve-out — see @protostar/paths Carve-Out section) | scope-ceiled to path resolution |
    | network | `@protostar/dogpile-adapter` | network (no fs) | ships `no-fs.contract.test.ts` |
    | network | `@protostar/delivery-runtime` | network (no fs) | ships `no-fs.contract.test.ts` + `no-merge.contract.test.ts` |
    | network | `@protostar/lmstudio-adapter` | network (no fs) | ships `no-fs.contract.test.ts` |
    | network | `@protostar/evaluation-runner` | network (no fs) | ships `no-fs.contract.test.ts`; coordinates judges via `dogpile-adapter` |
    | pure | `@protostar/artifacts` | none | |
    | pure | `@protostar/authority` | none | |
    | pure | `@protostar/delivery` | none | owns delivery contracts (network in `delivery-runtime`) |
    | pure | `@protostar/dogpile-types` | none | type-only re-export shim |
    | pure | `@protostar/evaluation` | none | |
    | pure | `@protostar/execution` | none | |
    | pure | `@protostar/fixtures` | none | |
    | pure | `@protostar/intent` | none | |
    | pure | `@protostar/mechanical-checks` | none | uses injected fs/subprocess capabilities |
    | pure | `@protostar/planning` | none | |
    | pure | `@protostar/policy` | none | |
    | pure | `@protostar/repair` | none | |
    | pure | `@protostar/review` | none | |
    | test-only | `@protostar/admission-e2e` | none | may depend on any tier; nothing may depend on it |

    ### Tier Edge Rules

    Enforced by `tier-conformance.contract.test.ts`:

    - `pure` may NOT depend on `network`, `fs`, or `orchestration`
    - `network` may NOT depend on `fs` (except for type-only imports — flagged if encountered)
    - `orchestration` may depend on anything
    - `test-only` may depend on anything; nothing may depend on `test-only`
    - Network-tier packages MUST ship a local `no-fs.contract.test.ts`
    - All `packages/*` + `apps/factory-cli` manifests MUST declare `protostar.tier`
    ```

    Additionally, KEEP the existing `## @protostar/paths Carve-Out` section verbatim — that prose is referenced from the tier table above. Do NOT add forbidden-edge prose for back-edges here; Plan 7 owns that addition.

    Pure-tier list MUST exactly match Task 1's list of 13 packages. Network-tier list MUST exactly match Task 3's list of 4 packages.
  </action>
  <verify>
    <automated>grep -q "protostar.tier" AGENTS.md && grep -q "test-only" AGENTS.md && grep -q "@protostar/lmstudio-adapter" AGENTS.md && grep -q "@protostar/admission-e2e" AGENTS.md && grep -q "@protostar/paths Carve-Out" AGENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md `## Authority Tiers` section lists all 21 packages (20 packages + apps/factory-cli) by name.
    - All five tier values appear: `orchestration`, `fs`, `network`, `pure`, `test-only`.
    - `@protostar/paths Carve-Out` section is preserved verbatim (no edits).
    - Tier field schema fragment includes the literal string `"protostar": { "tier":` followed by the union of all five values.
    - Tier edge rules section includes the explicit rule "Nothing may depend on `test-only`".
    - `grep -c "@protostar/" AGENTS.md` returns at least 18 (one per @protostar/* package).
  </acceptance_criteria>
  <done>AGENTS.md tier table is complete + tier field schema is documented + paths carve-out preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → conformance gate | Declared tier becomes load-bearing input to Plan 4's enforcement test |

## STRIDE Threat Register

No new attack surface — manifest + documentation changes only. No new code paths, no new network egress, no new subprocess invocations, no secrets handling. The added `protostar.tier` field is read-only metadata consumed by Plan 4's contract test.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1.1-01 | T (tampering) | `protostar.tier` field on package.json | mitigate | Plan 4's conformance gate fails CI if any package omits the field or declares an unknown tier value; tampering is caught at `pnpm verify` time |
</threat_model>

<verification>
Per-task verify commands run independently. Phase-level: `pnpm install` succeeds (no schema validation breakage). All 21 manifests parse as valid JSON. AGENTS.md remains markdown-valid (no broken sections).
</verification>

<success_criteria>
- All 21 package.json files contain `protostar.tier` matching the tier table in AGENTS.md
- AGENTS.md table covers every package with no gaps
- `@protostar/paths` is in fs tier with carve-out annotation; carve-out section preserved
- `pnpm install` succeeds; no test or build regressions (this plan is metadata-only)
- BOUNDARY-01 + BOUNDARY-02 are observable: every manifest declares its tier; AGENTS.md documents the schema
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-boundary-hygiene-pass/10.1-01-SUMMARY.md` summarizing manifest changes and pointing Plan 4 at the new tier field as its source of truth.
</output>
