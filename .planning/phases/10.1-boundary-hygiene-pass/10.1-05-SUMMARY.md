---
phase: 10.1-boundary-hygiene-pass
plan: 05
subsystem: boundary-hygiene
tags: [manifest-hygiene, knip, exports]
key-files:
  created: [tools/check-subpath-exports.ts]
  modified: [package.json, knip.json, packages/*/package.json]
metrics:
  subpath_checker_added: 1
---

# Plan 10.1.5 Summary — Manifest Hygiene Sweep

## What Changed

Completed the manifest sweep by adding Node 22 engines to published packages, adding or preserving `sideEffects: false` on pure/test-only packages, omitting it from fs/network packages, and wiring `tools/check-subpath-exports.ts` into `pnpm run verify` before knip. The checker uses Node 22 type stripping and validates `@protostar/*/subpath` imports against package export maps.

## Verification

- `node --experimental-strip-types tools/check-subpath-exports.ts` — PASS
- `pnpm run verify` — PASS

## Deviations

Knip was already installed by prior Phase 10 work, so this plan extended the verify chain instead of reinstalling knip.

## Self-Check: PASSED
