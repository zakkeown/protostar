---
phase: 10.1-boundary-hygiene-pass
plan: 06
type: execute
wave: 3
depends_on: [10.1.1-tier-classification]
files_modified:
  - apps/factory-cli/package.json
  - tools/factory-cli-pack-smoke.sh
  - .gitignore
autonomous: true
requirements: [BOUNDARY-10]

must_haves:
  truths:
    - "@protostar/factory-cli is publish-ready: private:false + publishConfig + engines.node>=22"
    - "Six existing subpath exports are preserved verbatim"
    - "tools/factory-cli-pack-smoke.sh packs the CLI, installs the tarball into a temp prefix, and verifies protostar-factory --help exits 0"
  artifacts:
    - path: "tools/factory-cli-pack-smoke.sh"
      provides: "Manual pack+install+invocation smoke for factory-cli publish wiring"
      min_lines: 25
  key_links:
    - from: "apps/factory-cli/package.json bin"
      to: "dist/main.js"
      via: "post-install --help invocation"
      pattern: "protostar-factory"
---

<objective>
Wire `apps/factory-cli` for publish as `@protostar/factory-cli` (per CONTEXT D-Q18 + Phase 10 Q-17). Includes a manual pack+install smoke script.

Purpose: BOUNDARY-10. Single-app scope; runs parallel with Plans 3/4/5 (touches different files).
Output: 1 modified manifest + 1 new shell script + .gitignore entry for pack tarballs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-boundary-hygiene-pass/10.1-RESEARCH.md
@.planning/phases/10.1-boundary-hygiene-pass/10.1-CONTEXT.md
@apps/factory-cli/package.json

<interfaces>
Current state (RESEARCH Task F — VERIFIED):
- private: true (needs flip)
- bin: { "protostar-factory": "./dist/main.js" } — already correct
- No publishConfig (needs `{ "access": "public" }`)
- No engines (needs `{ "node": ">=22" }`)
- Six subpath exports already defined: ./exit-codes, ./inspect-types, ./io, ./pile-persistence, ./refusals-index, ./status-types — preserve all
- dist/main.js exists after `tsc -b` (Phase 9 verification confirmed)
- Plan 1 already added protostar.tier: "orchestration" — preserve

Pack-smoke flow (CONTEXT D-Q18 fallback if CI is fragile):
1. pnpm --filter @protostar/factory-cli build
2. pnpm --filter @protostar/factory-cli pack produces protostar-factory-<version>.tgz at workspace root
3. Use temp dir + isolated prefix (do NOT pollute global npm)
4. Invoke installed binary with --help; assert exit 0 and stdout includes a known help-string fragment
5. Tear down temp dir
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip factory-cli to publish-ready manifest</name>
  <read_first>
    - apps/factory-cli/package.json (current full state)
    - packages/fixtures/package.json (only existing public-template in repo)
  </read_first>
  <files>apps/factory-cli/package.json</files>
  <action>
    Modify apps/factory-cli/package.json to be publish-ready as @protostar/factory-cli:

    1. Change `"private": true` to `"private": false`.
    2. Add `"publishConfig": { "access": "public" }` after `"private"`.
    3. Add `"engines": { "node": ">=22" }`.
    4. Verify the package name is exactly `"@protostar/factory-cli"`; if not, STOP and escalate.
    5. Preserve verbatim: bin, the six subpath exports, protostar.tier: "orchestration", dependencies, devDependencies, scripts.

    Do NOT add a sideEffects field (orchestration tier does not declare it per Plan 5's tier rule map).

    After editing, run `pnpm install` to update the lockfile.
  </action>
  <verify>
    <automated>pnpm install &amp;&amp; node -e "const m=require('./apps/factory-cli/package.json'); const fail=[]; if(m.private!==false)fail.push('private'); if(m.publishConfig&amp;&amp;m.publishConfig.access!=='public')fail.push('publishConfig'); if(!m.publishConfig)fail.push('publishConfig-missing'); if(!m.engines||m.engines.node!=='>=22')fail.push('engines'); if(!m.bin||m.bin['protostar-factory']!=='./dist/main.js')fail.push('bin'); if(!m.protostar||m.protostar.tier!=='orchestration')fail.push('tier'); const expected=['./exit-codes','./inspect-types','./io','./pile-persistence','./refusals-index','./status-types']; for(const e of expected){if(!m.exports||!m.exports[e])fail.push('exports '+e)} if(fail.length){console.error('FAIL',fail);process.exit(1)} console.log('OK')" &amp;&amp; pnpm --filter @protostar/factory-cli build</automated>
  </verify>
  <acceptance_criteria>
    - apps/factory-cli/package.json has private:false, publishConfig.access==="public", engines.node===">=22".
    - bin entry unchanged: protostar-factory -> ./dist/main.js.
    - All six subpath exports preserved.
    - protostar.tier still "orchestration".
    - pnpm install succeeds; lockfile updates.
    - pnpm --filter @protostar/factory-cli build succeeds.
    - pnpm run verify still green.
  </acceptance_criteria>
  <done>factory-cli manifest flipped publish-ready; existing subpath exports + bin + tier preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add pack+install smoke script + ignore tarballs</name>
  <read_first>
    - .gitignore (root)
    - apps/factory-cli/dist/main.js (presence check only — confirm build artifact exists after Task 1's build)
  </read_first>
  <files>tools/factory-cli-pack-smoke.sh, .gitignore</files>
  <action>
    Create tools/factory-cli-pack-smoke.sh implementing CONTEXT D-Q18's manual smoke:

    1. set -euo pipefail.
    2. Resolve repo root via git rev-parse --show-toplevel.
    3. mktemp -d for an isolated install prefix; trap rm -rf for cleanup on exit.
    4. (cd "$REPO_ROOT" && pnpm --filter @protostar/factory-cli build).
    5. (cd "$REPO_ROOT/apps/factory-cli" && pnpm pack --pack-destination "$TMP") to drop the tarball into TMP.
    6. Find the tarball: TARBALL=$(ls "$TMP"/protostar-factory-*.tgz | head -n1).
    7. Install into the temp prefix: (cd "$TMP" && npm install --prefix "$TMP/install" "$TARBALL").
    8. Invoke: "$TMP/install/node_modules/.bin/protostar-factory" --help >"$TMP/help.out" 2>&1.
    9. Assert exit 0 and grep for a known fragment from the existing help fixture (Phase 9 Plan 11 captured help fixtures — read one to pick a stable token; e.g. literal "protostar-factory" or "Usage:" — confirm via the fixture file before pinning).
    10. Echo a success line and exit 0.

    Make it executable: chmod +x tools/factory-cli-pack-smoke.sh.

    Add to .gitignore (if not already): a line ignoring `*.tgz` AND a line ignoring `apps/factory-cli/protostar-factory-*.tgz` (in case pnpm pack drops the tarball into the package dir despite --pack-destination).

    Document in the script header (top comment) that this is a manual smoke (not CI-wired in 10.1 per CONTEXT D-Q18 fallback) and that running it requires npm available locally.
  </action>
  <verify>
    <automated>test -x tools/factory-cli-pack-smoke.sh &amp;&amp; grep -q "protostar-factory" tools/factory-cli-pack-smoke.sh &amp;&amp; grep -q "*.tgz" .gitignore &amp;&amp; bash tools/factory-cli-pack-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - tools/factory-cli-pack-smoke.sh exists, is executable, and contains the literal tokens "pnpm pack", "npm install", "--help", "protostar-factory".
    - .gitignore contains *.tgz (or equivalent broader pattern that catches pack output).
    - Running the script end-to-end exits 0 on a clean checkout.
    - Script uses mktemp + trap for cleanup; no global npm pollution.
    - Script grep-asserts a stable help-string fragment (validated against an existing Phase 9 help fixture).
  </acceptance_criteria>
  <done>factory-cli pack+install smoke runs locally and exits 0; manual run path documented in script header per CONTEXT D-Q18 fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| factory-cli npm publish | Package becomes installable globally; supply-chain surface |
| pack-smoke script | Runs npm install with arbitrary tarball path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1.6-01 | T (tampering) | factory-cli on npm | mitigate | Phase 10 DOG-07 (changesets + release pipeline) is the gate to actual npm publish. 10.1 only flips `private:false`; no `pnpm publish` is invoked. publishConfig.access:"public" is required to publish a scoped package — explicit, not implicit |
| T-10.1.6-02 | E (elevation of privilege) | npm install of local tarball in pack-smoke | accept | Tarball is locally produced from this repo's source; install goes into a mktemp prefix, not global. Script aborts on any non-zero exit. Operator-run, not CI-run |
| T-10.1.6-03 | I (information disclosure) | bin script may inadvertently print env vars in --help | mitigate | The --help output is captured to TMP/help.out and grep-checked against a stable fragment; no env propagation. Phase 9 already validated --help fixtures |
</threat_model>

<verification>
- factory-cli manifest fields verified by node -e check.
- pack-smoke script run end-to-end as a verify command.
- pnpm run verify still green.
- BOUNDARY-10 observable.
</verification>

<success_criteria>
- @protostar/factory-cli is publish-ready (manifest fields).
- Manual pack-smoke validates the binary works post-install.
- Existing six subpath exports + bin + tier are preserved.
- Plan 4's conformance gate sees factory-cli with the orchestration tier and complete publish fields.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-boundary-hygiene-pass/10.1-06-SUMMARY.md` recording manifest changes, the help-fragment chosen for the smoke assertion, and a note that actual npm publish is gated on Phase 10 DOG-07.
</output>
