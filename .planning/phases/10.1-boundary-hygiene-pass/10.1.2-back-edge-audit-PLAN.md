---
phase: 10.1-boundary-hygiene-pass
plan: 02
type: execute
wave: 2
depends_on: [10.1.1-tier-classification]
files_modified:
  - packages/intent/package.json
  - packages/repo/package.json
  - packages/review/tsconfig.json
  - packages/intent/tsconfig.json
  - packages/repo/tsconfig.json
  - AGENTS.md
autonomous: true
requirements: [BOUNDARY-11, BOUNDARY-12]

must_haves:
  truths:
    - "Phantom dep `@protostar/authority` is removed from packages/intent/package.json"
    - "Phantom deps `@protostar/authority` and `@protostar/paths` are removed from packages/repo/package.json"
    - "packages/review/tsconfig.json references `@protostar/delivery` and `@protostar/repair`"
    - "AGENTS.md documents three accepted back-edges: review->delivery (type bridge), review->repair (loop body), delivery-runtime->review (brand consumption)"
    - "No back-edge produces an unexplained import in source"
  artifacts:
    - path: "AGENTS.md"
      provides: "Forbidden Edges + Accepted Back-Edges section"
      contains: "Accepted Back-Edges"
  key_links:
    - from: "packages/review/tsconfig.json"
      to: "packages/delivery + packages/repair"
      via: "references[]"
      pattern: "\\.\\./delivery\"|\\.\\./repair\""
---

<objective>
Resolve the four flagged dependency back-edges using RESEARCH.md's verified grep findings (D-Q08 + D-Q09 + D-Q10 deferred to planner). Three are PHANTOM deps (zero source imports — remove from manifests); three are real edges that the planner classifies as accept + document. Also fix verified tsconfig project-reference drift on `intent`, `repo`, and `review`.

Purpose: BOUNDARY-11 (back-edge audit) + BOUNDARY-12 (tsconfig drift fix). Lands BEFORE Plan 5 (manifest hygiene) so the conformance gate (Plan 4) sees a clean dep graph.
Output: 3 package.json edits (removing 3 phantom deps), 3 tsconfig.json edits (drift fix), AGENTS.md addition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-boundary-hygiene-pass/10.1-RESEARCH.md
@.planning/phases/10.1-boundary-hygiene-pass/10.1-CONTEXT.md
@AGENTS.md

<interfaces>
<!-- All facts below are VERIFIED in RESEARCH.md (Back-Edge Audit + tsconfig Drift sections). -->

PHANTOM DEPS (zero source imports — safe to remove):
- packages/intent/package.json declares `@protostar/authority`. Source grep: ZERO hits.
- packages/repo/package.json declares `@protostar/authority`. Source grep: ZERO hits.
- packages/repo/package.json declares `@protostar/paths`. Source grep: ZERO hits.

REAL BACK-EDGES (accept + document):
- review -> delivery: type-only `AuthorizationPayload` + `isAuthorizationPayload` from `@protostar/delivery/authorization-payload` subpath. File: packages/review/src/delivery-authorization.ts.
- review -> repair: runtime `synthesizeRepairPlan`, `computeRepairSubgraph` from `@protostar/repair`. File: packages/review/src/run-review-repair-loop.ts.
- delivery-runtime -> review: type `DeliveryAuthorization` + runtime `mintDeliveryAuthorization` (test fixture only) from `@protostar/review`. File: packages/delivery-runtime/src/execute-delivery.ts.

TSCONFIG DRIFT (verified):
- packages/intent/tsconfig.json references = []. After phantom dep removal, refs match (still empty). No edit needed beyond verifying parity post-Task 1.
- packages/repo/tsconfig.json references = []. After phantom dep removal, refs match. No edit needed beyond verification.
- packages/review/tsconfig.json missing refs to `delivery` and `repair`. ADD them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove phantom deps from intent + repo manifests</name>
  <read_first>
    - packages/intent/package.json
    - packages/repo/package.json
    - .planning/phases/10.1-boundary-hygiene-pass/10.1-RESEARCH.md (Back-Edge Audit section, Edge 1 + tsconfig drift table)
  </read_first>
  <files>packages/intent/package.json, packages/repo/package.json</files>
  <action>
    Re-verify the grep results before editing (RESEARCH evidence is from 2026-04-28 and the source has not changed):
    ```
    grep -rn "from.*@protostar/authority" packages/intent/src/   # MUST be zero
    grep -rn "from.*@protostar/authority\|from.*@protostar/paths" packages/repo/src/   # MUST be zero
    ```
    If any grep returns a hit, STOP and escalate (research findings would be invalidated).

    If grep is clean:
    1. In `packages/intent/package.json`, remove the `"@protostar/authority": "workspace:*"` line from `dependencies` (per BOUNDARY-11 phantom resolution).
    2. In `packages/repo/package.json`, remove both `"@protostar/authority": "workspace:*"` and `"@protostar/paths": "workspace:*"` from `dependencies`.

    Do NOT touch `tsconfig.json` of either package in this task (Task 2 handles refs). Do NOT remove any non-phantom dep.
  </action>
  <verify>
    <automated>node -e "const i=require('./packages/intent/package.json'); const r=require('./packages/repo/package.json'); if(i.dependencies?.['@protostar/authority']){console.error('intent still has phantom authority dep');process.exit(1)} if(r.dependencies?.['@protostar/authority']||r.dependencies?.['@protostar/paths']){console.error('repo still has phantom deps',r.dependencies);process.exit(1)} console.log('OK')" && grep -rn "from.*@protostar/authority" packages/intent/src/ ; test $? -ne 0 && grep -rn "from.*@protostar/authority\|from.*@protostar/paths" packages/repo/src/ ; test $? -ne 0 && echo VERIFIED-NO-IMPORTS</automated>
  </verify>
  <acceptance_criteria>
    - `packages/intent/package.json` `dependencies` does NOT contain `@protostar/authority`.
    - `packages/repo/package.json` `dependencies` does NOT contain `@protostar/authority` or `@protostar/paths`.
    - `pnpm install` completes successfully (lockfile updates accepted).
    - Grep over `packages/intent/src/` for `@protostar/authority` returns zero matches.
    - Grep over `packages/repo/src/` for `@protostar/authority` and `@protostar/paths` returns zero matches.
    - `pnpm --filter @protostar/intent build` and `pnpm --filter @protostar/repo build` succeed.
  </acceptance_criteria>
  <done>3 phantom deps removed; both packages still build; lockfile updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing tsconfig refs to review</name>
  <read_first>
    - packages/review/package.json
    - packages/review/tsconfig.json
    - packages/review/src/delivery-authorization.ts (lines 1-10)
    - packages/review/src/run-review-repair-loop.ts (lines 1-10)
  </read_first>
  <files>packages/review/tsconfig.json</files>
  <action>
    Add two project references to `packages/review/tsconfig.json` so its TS project graph matches its declared `@protostar/*` deps (per BOUNDARY-12). The package depends on `@protostar/delivery` and `@protostar/repair` in `package.json`, but `tsconfig.json` is missing both refs. Add them to the `references` array. Use the relative-path style already present in the file (e.g. `{ "path": "../delivery" }` and `{ "path": "../repair" }`).

    Do NOT reorder the existing references array (Plan 4 conformance gate uses set-equality, not array-equality, per CONTEXT D-Q13 with the planner's set-equality clarification — see Plan 4 for rationale).

    For `intent` and `repo` tsconfig.json: per RESEARCH the references arrays are already empty; after Task 1 removes phantom deps the dep set is also empty (or excludes the removed ones). No tsconfig edit needed for those two — verify post-Task 1 that intent and repo tsconfigs still validate via `tsc -b --dry`.
  </action>
  <verify>
    <automated>node -e "const t=JSON.parse(require('fs').readFileSync('packages/review/tsconfig.json','utf8').replace(/\/\/.*$/gm,'').replace(/\/\*[\s\S]*?\*\//g,'')); const refs=(t.references||[]).map(r=>r.path); const need=['../delivery','../repair']; const miss=need.filter(n=>!refs.includes(n)); if(miss.length){console.error('missing refs',miss);process.exit(1)} console.log('OK')" && pnpm --filter @protostar/review build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/review/tsconfig.json` `references[]` contains both `{ "path": "../delivery" }` and `{ "path": "../repair" }`.
    - `pnpm --filter @protostar/review build` succeeds.
    - `pnpm -w exec tsc --build packages/review` succeeds with no project-graph warning.
    - JSON-with-comments form preserved (do not strip comments).
  </acceptance_criteria>
  <done>review tsconfig refs match its @protostar/* deps; project graph clean.</done>
</task>

<task type="auto">
  <name>Task 3: Document accepted back-edges in AGENTS.md</name>
  <read_first>
    - AGENTS.md (current state after Plan 1 — has new Authority Tiers table)
    - .planning/phases/10.1-boundary-hygiene-pass/10.1-RESEARCH.md (Back-Edge Audit Edges 2/3/4)
  </read_first>
  <files>AGENTS.md</files>
  <action>
    Append a new section `## Accepted Back-Edges` immediately after the `## Authority Tiers` section (and before the `## Development Rules` section). Use this exact content:

    ```markdown
    ## Accepted Back-Edges

    The following cross-package edges are sanctioned exceptions to the tier graph's natural top-down direction. Each is reviewed and accepted because the alternative (extracting a neutral types package, or moving runtime symbols) over-engineers a one-purpose seam. The workspace conformance gate (`tier-conformance.contract.test.ts`) recognizes these by package-pair allowlist; any new back-edge MUST be added here with rationale before the gate accepts it.

    ### review → delivery (type bridge)

    `@protostar/review` imports `AuthorizationPayload` (type) and `isAuthorizationPayload` (runtime validator) from `@protostar/delivery/authorization-payload`. The `delivery` package owns the persisted payload schema; `review` owns the brand minted from it during `reAuthorizeFromPayload`. Permitted for the authorization payload type-bridge only.

    ### review → repair (loop body call site)

    `@protostar/review/run-review-repair-loop.ts` calls `synthesizeRepairPlan` and `computeRepairSubgraph` from `@protostar/repair`. The review-repair loop is the sanctioned call site for repair synthesis; `repair` MUST NOT depend on `review`.

    ### delivery-runtime → review (brand consumption)

    `@protostar/delivery-runtime` imports `DeliveryAuthorization` (type, brand) and `mintDeliveryAuthorization` (test fixture only) from `@protostar/review`. `review` is the sole issuer of the `DeliveryAuthorization` brand. Production callers consume it as a type; only test fixtures use the runtime mint.

    ### Phantom Edges Removed (2026-04-28, Phase 10.1)

    Three previously-declared workspace deps had ZERO source imports and were removed in Phase 10.1:
    - `@protostar/intent` → `@protostar/authority` (declared in `intent/package.json`, never imported)
    - `@protostar/repo` → `@protostar/authority` (declared in `repo/package.json`, never imported — types are re-declared locally to avoid a circular ref)
    - `@protostar/repo` → `@protostar/paths` (declared in `repo/package.json`, never imported)

    These were stale skeleton declarations, not real edges. They are documented here so a future contributor doesn't re-add them.
    ```

    Insert this section at the correct location (after Authority Tiers, before Development Rules). Do NOT modify the Authority Tiers table or the @protostar/paths Carve-Out section.
  </action>
  <verify>
    <automated>grep -q "## Accepted Back-Edges" AGENTS.md && grep -q "review → delivery (type bridge)" AGENTS.md && grep -q "review → repair (loop body" AGENTS.md && grep -q "delivery-runtime → review (brand" AGENTS.md && grep -q "Phantom Edges Removed" AGENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains a new `## Accepted Back-Edges` section between Authority Tiers and Development Rules.
    - Section enumerates all three accepted edges with rationale.
    - Section enumerates the three removed phantom edges.
    - Authority Tiers table from Plan 1 unchanged.
    - paths Carve-Out section unchanged.
  </acceptance_criteria>
  <done>Three accepted back-edges and three removed phantoms are documented; AGENTS.md is the single source of truth for sanctioned tier-graph exceptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm-lockfile | Phantom-dep removal mutates the lockfile; supply-chain integrity must hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1.2-01 | T (tampering) | pnpm-lock.yaml after dep removal | accept | Phantom-dep removal cannot introduce new code paths — the deps were never imported. Lockfile delta is verifiable: only the three workspace edges disappear; no new external resolution. |
| T-10.1.2-02 | I (information disclosure) | AGENTS.md back-edge documentation | accept | Documentation only — no secrets, no operational state. The text describes existing source structure visible in the public repo. |
</threat_model>

<verification>
- All three phantom deps confirmed gone from manifests; corresponding source imports remain absent.
- `pnpm --filter @protostar/intent build`, `pnpm --filter @protostar/repo build`, `pnpm --filter @protostar/review build` all succeed.
- `pnpm install` completes without churn.
- `pnpm run verify` still green.
- AGENTS.md has the new Accepted Back-Edges section in the correct location.
</verification>

<success_criteria>
- BOUNDARY-11 satisfied: per-edge resolution shipped (3 phantoms removed, 3 real edges accepted + documented).
- BOUNDARY-12 satisfied: review tsconfig refs match its @protostar/* deps; intent + repo tsconfig refs are empty (matching their now-empty @protostar/* dep set).
- No source code changes — manifests, tsconfigs, and docs only.
- Plan 4's conformance gate (deps ≅ refs, set-equality) will pass on intent, repo, review.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-boundary-hygiene-pass/10.1-02-SUMMARY.md` listing the three removed phantoms, the three accepted back-edges, and the tsconfig refs added to review.
</output>
