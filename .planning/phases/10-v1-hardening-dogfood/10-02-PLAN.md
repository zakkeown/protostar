---
phase: 10-v1-hardening-dogfood
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - packages/fixtures/package.json
  - packages/fixtures/tsconfig.json
  - packages/fixtures/tsconfig.build.json
  - packages/fixtures/src/index.ts
  - packages/fixtures/src/seeds/index.ts
  - packages/fixtures/src/seeds/button-color-hover.ts
  - packages/fixtures/src/seeds/card-shadow.ts
  - packages/fixtures/src/seeds/navbar-aria.ts
  - packages/fixtures/src/seeds/seed-library.test.ts
  - pnpm-workspace.yaml
  - tsconfig.json
  - pnpm-lock.yaml
  - apps/factory-cli/src/commands/prune.ts
  - apps/factory-cli/src/commands/prune.test.ts
autonomous: false
requirements: [DOG-03]
must_haves:
  truths:
    - "`@protostar/fixtures` package exists and exports `seedLibrary` with 3 seeds: button-color-hover, card-shadow, navbar-aria"
    - "Seed `button-color-hover` carries the verbatim DOG-03 intent text"
    - "A single end-to-end factory run against `../protostar-toy-ttt/` using seed `button-color-hover` opens a PR with green CI"
    - "Phase 9 `prune` subcommand scope includes `.protostar/dogfood/` paths (extension landed in this plan, not blocked on Phase 9)"
    - "Phase 7 deferred toy-repo PR + screenshot evidence is captured by this run"
  artifacts:
    - path: "packages/fixtures/src/seeds/button-color-hover.ts"
      provides: "DOG-03 baseline seed with verbatim 2026-04-24 wording"
      contains: "Change the primary button color and add a hover state"
    - path: "packages/fixtures/src/index.ts"
      provides: "Public surface: `seedLibrary`, `getSeed(id)`, types"
      exports: ["seedLibrary", "getSeed", "Seed"]
    - path: ".planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-url.txt"
      provides: "PR URL artifact captured during the smoke run (operator-checkpointed)"
  key_links:
    - from: "scripts/dogfood.sh (10-08)"
      to: "packages/fixtures/src/seeds/"
      via: "round-robin import"
      pattern: "@protostar/fixtures"
    - from: "apps/factory-cli/src/commands/prune.ts"
      to: ".protostar/dogfood/"
      via: "scan path enumeration"
      pattern: "\\.protostar/dogfood"
---

<objective>
Build the 3-seed fixture library at `packages/fixtures/`, run a single end-to-end factory invocation against the toy repo using the verbatim DOG-03 seed, and (because Phase 9 prune may not have shipped dogfood-scope yet per STATE.md) extend `prune` to scan `.protostar/dogfood/`.

Purpose: Plan 10-02 is the de-facto Phase 7 verifier — the first real factory PR against a real repo. If this plan fails, wave 2 does not start. Establishes the seed library every later Phase 10 plan depends on.
Output: New `@protostar/fixtures` workspace package, one merged-or-open PR on `zkeown/protostar-toy-ttt`, prune scope extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md
@.planning/phases/09-operator-surface-resumability/09-CONTEXT.md
@AGENTS.md
@apps/factory-cli/src/commands/prune.ts

<interfaces>
<!-- Seed library shape (this plan creates these types — Wave 0 of the plan) -->

```typescript
// packages/fixtures/src/seeds/index.ts
export type SeedArchetype = 'cosmetic-tweak';

export interface Seed {
  id: string;            // e.g., 'button-color-hover'
  intent: string;        // operator-facing seed text passed to factory
  archetype: SeedArchetype;
  notes: string;         // human notes for fixture maintainers
}

export const seedLibrary: readonly Seed[] = [...];
export function getSeed(id: string): Seed; // throws on miss
export function listSeedIds(): readonly string[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold @protostar/fixtures package + author 3-seed library (TDD)</name>
  <read_first>
    - packages/paths/package.json (template for a tiny pure package — copy strict ts setup)
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-04 verbatim seed text; Q-05 package location)
    - pnpm-workspace.yaml (workspace registration)
    - tsconfig.json (root project references)
  </read_first>
  <files>
    packages/fixtures/package.json,
    packages/fixtures/tsconfig.json,
    packages/fixtures/tsconfig.build.json,
    packages/fixtures/src/index.ts,
    packages/fixtures/src/seeds/index.ts,
    packages/fixtures/src/seeds/button-color-hover.ts,
    packages/fixtures/src/seeds/card-shadow.ts,
    packages/fixtures/src/seeds/navbar-aria.ts,
    packages/fixtures/src/seeds/seed-library.test.ts,
    pnpm-workspace.yaml,
    tsconfig.json
  </files>
  <behavior>
    - Test 1: `seedLibrary` length === 3
    - Test 2: `getSeed('button-color-hover').intent === 'Change the primary button color and add a hover state'` (VERBATIM)
    - Test 3: every seed has `archetype === 'cosmetic-tweak'`
    - Test 4: `getSeed('does-not-exist')` throws with message containing the unknown id
    - Test 5: `listSeedIds()` returns frozen ordered array `['button-color-hover','card-shadow','navbar-aria']`
    - Test 6: seed objects are deeply frozen (e.g., `Object.isFrozen(seedLibrary)` &amp;&amp; `Object.isFrozen(seedLibrary[0])`)
  </behavior>
  <action>
    1. Create `packages/fixtures/package.json` with `"name": "@protostar/fixtures"`, `"private": false` (Q-17), `"publishConfig": { "access": "public" }`, ESM (`"type": "module"`), `"exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } }`, scripts: `build` (`tsc -b tsconfig.build.json`), `test` (`node --test dist/**/*.test.js`).
    2. Create `tsconfig.json` extending root strict config; `tsconfig.build.json` composite, `outDir: dist`.
    3. Create `src/seeds/<seed-id>.ts` for each of the three IDs. Each exports a `const` typed `Seed` and `Object.freeze`s it. Verbatim intent texts:
       - `button-color-hover`: `Change the primary button color and add a hover state`
       - `card-shadow`: `Add a subtle shadow and rounded corners to the card component`
       - `navbar-aria`: `Add aria-label attributes to nav-bar buttons for accessibility`
    4. Create `src/seeds/index.ts` exporting `Seed`, `SeedArchetype`, `seedLibrary` (frozen array of 3), `getSeed(id)`, `listSeedIds()`.
    5. Create `src/index.ts` re-exporting all of `src/seeds/index.ts`.
    6. Create `src/seeds/seed-library.test.ts` with the 6 behaviors above using `node:test`.
    7. Add `packages/fixtures` to `pnpm-workspace.yaml` (already covered by `packages/*` glob if present — verify; otherwise add).
    8. Add `{ "path": "packages/fixtures" }` reference to root `tsconfig.json` `references` array.
    9. Run `pnpm install`, `pnpm --filter @protostar/fixtures build`, `pnpm --filter @protostar/fixtures test` — all green.
    10. Run `pnpm run verify` (root) — must remain green.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @protostar/fixtures test` passes 6 tests (Test 2 grep-locks the verbatim string)
    - `grep -F "Change the primary button color and add a hover state" packages/fixtures/src/seeds/button-color-hover.ts` exits 0
    - `grep -c "as const" packages/fixtures/src/seeds/index.ts` &gt;= 1 (Q-05 typed accessors)
    - `pnpm run verify` exits 0
    - `cat packages/fixtures/package.json | jq -r '.publishConfig.access'` returns `public`
  </acceptance_criteria>
  <verify>
    <automated>pnpm install &amp;&amp; pnpm --filter @protostar/fixtures build &amp;&amp; pnpm --filter @protostar/fixtures test &amp;&amp; grep -qF "Change the primary button color and add a hover state" packages/fixtures/src/seeds/button-color-hover.ts &amp;&amp; pnpm run verify</automated>
  </verify>
  <done>3-seed library committed; verbatim DOG-03 string locked; verify green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Phase 9 `prune` scope to `.protostar/dogfood/`</name>
  <read_first>
    - apps/factory-cli/src/commands/prune.ts (current scope; Phase 9 implementation — may not include dogfood paths)
    - apps/factory-cli/src/commands/prune.test.ts
    - .planning/phases/09-operator-surface-resumability/09-CONTEXT.md (Q-22 prune scope, active-status guard, lineage preservation)
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (carried_forward: prune extends to `.protostar/dogfood/`)
  </read_first>
  <files>apps/factory-cli/src/commands/prune.ts, apps/factory-cli/src/commands/prune.test.ts</files>
  <behavior>
    - Test 1: `prune --older-than 1d --dry-run` reports candidate paths under `.protostar/dogfood/` when those dirs exist with old mtime
    - Test 2: `prune --older-than 1d` actually deletes `.protostar/dogfood/<sessionId>/` whose mtime exceeds the cap
    - Test 3: `prune` does NOT delete `.protostar/refusals.jsonl` (append-only preservation; same guard as Phase 9)
    - Test 4: `prune` does NOT delete `.protostar/evolution/<lineageId>.jsonl` (lineage preservation; same guard as Phase 9)
    - Test 5: `prune` does NOT delete a `.protostar/dogfood/<sessionId>/` whose `cursor` shows an in-progress session (active-status guard)
  </behavior>
  <action>
    1. If Phase 9's `prune` already includes `.protostar/dogfood/` in its scan list, this task is a no-op verification — confirm by reading `prune.ts` for `dogfood` token; if present, skip code edits but still add Test 1 + Test 2 to lock the contract.
    2. Otherwise, add `.protostar/dogfood/` as a scan root (mirror existing `.protostar/runs/` handling). Active-status guard: a dogfood session is "active" iff its `cursor` JSON has `completed &lt; totalRuns`. Skip its directory.
    3. Append-only files (`refusals.jsonl`, `evolution/*.jsonl`) remain protected by the same exclusion list Phase 9 already enforces — do not regress.
    4. Add the 5 behavior tests to `prune.test.ts` using `node:test` + tmp-fs fixture (mirror existing tests in this file).
    5. Run `pnpm --filter @protostar/factory-cli build &amp;&amp; pnpm --filter @protostar/factory-cli test`.
  </action>
  <acceptance_criteria>
    - `grep -c "dogfood" apps/factory-cli/src/commands/prune.ts` &gt;= 1
    - `pnpm --filter @protostar/factory-cli test --grep "prune"` passes new tests
    - `pnpm run verify` exits 0
    - No regression: existing prune tests still pass
  </acceptance_criteria>
  <verify>
    <automated>grep -q "dogfood" apps/factory-cli/src/commands/prune.ts &amp;&amp; pnpm --filter @protostar/factory-cli build &amp;&amp; pnpm --filter @protostar/factory-cli test &amp;&amp; pnpm run verify</automated>
  </verify>
  <done>Prune scans dogfood dirs; active sessions protected; append-only preserved</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT): Run DOG-03 single end-to-end factory invocation</name>
  <read_first>
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-10 pass criterion: PR opened AND CI green)
    - .planning/phases/10-v1-hardening-dogfood/10-01-PLAN.md (toy repo + PAT prerequisites)
    - apps/factory-cli/dist/main.js or `pnpm run factory` entrypoint
  </read_first>
  <what-built>One real factory run against `../protostar-toy-ttt/` using the verbatim DOG-03 seed text, ending in an open PR with green CI.</what-built>
  <how-to-verify>
    1. Confirm prerequisites: `[ -n "$PROTOSTAR_DOGFOOD_PAT" ]` AND `gh repo view zkeown/protostar-toy-ttt` succeeds AND `pnpm run verify` is green AND LM Studio is running with the locked Qwen3-Coder + judge models loaded.
    2. From the protostar repo root, invoke the factory with the verbatim seed:
       ```
       export GITHUB_TOKEN="$PROTOSTAR_DOGFOOD_PAT"
       pnpm run factory -- run \
         --intent "Change the primary button color and add a hover state" \
         --target-repo "https://github.com/zkeown/protostar-toy-ttt.git" \
         --archetype cosmetic-tweak
       ```
       (exact flag names per Phase 9's `run` command; consult `pnpm run factory -- run --help` if names differ.)
    3. Wait for the run to complete (dark autonomy — no progress logs; final summary on stderr).
    4. Confirm a PR was opened: `gh pr list --repo zkeown/protostar-toy-ttt --state open --json url,headRefName --limit 5`. Capture the URL.
    5. Wait for CI to go green on that PR: `gh pr checks <pr-number> --repo zkeown/protostar-toy-ttt --watch`. Required check `build-and-test` must succeed within 10 minutes.
    6. Capture the PR URL into `.planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-url.txt`. Capture a screenshot of the rendered PR body into `.planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-screenshot.png` (Phase 7 deferred deliverable).
    7. Inspect the run bundle: `pnpm run factory -- inspect &lt;runId&gt; --json | jq '.manifest.status, .reviewVerdict'` — `status` should be `delivered` (or equivalent terminal-success per Phase 9 enum) and `reviewVerdict` should be `pass`.
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-url.txt` exists and contains a `https://github.com/zkeown/protostar-toy-ttt/pull/` URL
    - File `.planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-screenshot.png` exists (non-zero bytes)
    - `gh pr view $(cat .planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-url.txt) --json statusCheckRollup -q '.statusCheckRollup[] | select(.name=="build-and-test") | .conclusion'` returns `SUCCESS`
    - PR body references the run bundle (Phase 7 contract)
    - Local run bundle exists at `.protostar/runs/&lt;runId&gt;/manifest.json` with terminal success status
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-url.txt &amp;&amp; test -s .planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-screenshot.png &amp;&amp; grep -qE "^https://github\.com/zkeown/protostar-toy-ttt/pull/[0-9]+" .planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-url.txt &amp;&amp; [ "$(gh pr view "$(cat .planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-url.txt)" --json statusCheckRollup -q '[.statusCheckRollup[] | select(.name=="build-and-test") | .conclusion] | .[0]')" = "SUCCESS" ]</automated>
  </verify>
  <resume-signal>Type "approved" when PR URL captured and CI green; or describe failure mode (this is the Phase 7 catch-up — a failure here blocks wave 2)</resume-signal>
  <done>Real PR exists, CI green, evidence captured. Phase 7's deferred toy-repo deliverable is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Factory → toy repo | First real `git push` + Octokit PR open against an external public repo |
| PAT → environment | PAT in `GITHUB_TOKEN` env var for delivery-runtime |
| LM Studio → factory | Local model invocation; not a security boundary but a reliability one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | Branch name | mitigate | Phase 7 PR safety regex `^[a-zA-Z0-9._/-]+$` (PROJECT.md) — already enforced by delivery-runtime |
| T-10-02-02 | Information disclosure | PAT in run logs | mitigate | Phase 7's redaction sanitizer covers `GITHUB_TOKEN` — verified in Phase 7 review |
| T-10-02-03 | Denial of service | Open PR accumulation | accept | Q-03 branch-per-run-no-merge; operator manually closes batches |
| T-10-02-04 | Tampering | Prune deleting active session | mitigate | Active-status guard via cursor.completed&lt;totalRuns check (Task 2 Test 5) |
</threat_model>

<verification>
- Task 1: 6 unit tests pass; verbatim string grep-locked
- Task 2: prune scope includes `.protostar/dogfood/`; existing prune contracts intact
- Task 3 (checkpoint): PR exists with green CI; evidence files present
- `pnpm run verify` green after all tasks
</verification>

<success_criteria>
- `@protostar/fixtures` package shipping with 3-seed library
- DOG-03 verbatim seed text locked
- One green PR on toy repo (Phase 7 deferred work closed)
- Prune scope extended to dogfood paths (does not block on Phase 9 closure)
</success_criteria>

<output>
After completion, create `.planning/phases/10-v1-hardening-dogfood/10-02-SUMMARY.md` referencing the captured PR URL and confirming Phase 7 deferred deliverable is closed.
</output>
