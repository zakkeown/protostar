---
phase: 10-v1-hardening-dogfood
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - packages/fixtures/src/matrix/index.ts
  - packages/fixtures/src/matrix/get-matrix-row.ts
  - packages/fixtures/src/matrix/matrix.test.ts
  - packages/fixtures/__fixtures__/accepted/expectations.ts
  - packages/fixtures/__fixtures__/accepted/manifest.json
  - packages/fixtures/__fixtures__/accepted/review-gate.json
  - packages/fixtures/__fixtures__/ambiguous/expectations.ts
  - packages/fixtures/__fixtures__/ambiguous/manifest.json
  - packages/fixtures/__fixtures__/bad-plan/expectations.ts
  - packages/fixtures/__fixtures__/bad-plan/manifest.json
  - packages/fixtures/__fixtures__/failed-execution/expectations.ts
  - packages/fixtures/__fixtures__/failed-execution/manifest.json
  - packages/fixtures/__fixtures__/repaired-execution/expectations.ts
  - packages/fixtures/__fixtures__/repaired-execution/manifest.json
  - packages/fixtures/__fixtures__/repaired-execution/review-gate.json
  - packages/fixtures/__fixtures__/blocked-review/expectations.ts
  - packages/fixtures/__fixtures__/blocked-review/manifest.json
  - packages/fixtures/__fixtures__/blocked-review/review-gate.json
  - packages/fixtures/__fixtures__/pr-ready/expectations.ts
  - packages/fixtures/__fixtures__/pr-ready/manifest.json
  - packages/fixtures/__fixtures__/pr-ready/review-gate.json
  - scripts/regen-matrix.sh
  - packages/admission-e2e/src/fixture-matrix-coverage.contract.test.ts
  - packages/admission-e2e/src/fixture-matrix-age.contract.test.ts
  - packages/fixtures/src/index.ts
autonomous: true
requirements: [DOG-02]
must_haves:
  truths:
    - "Each of the 7 outcomes has a fixture row at `packages/fixtures/__fixtures__/&lt;outcome&gt;/`"
    - "Every row has `expectations.ts` declaring expected manifestStatus + reviewVerdict + triggeredBy"
    - "`getMatrixRow(outcome)` returns the typed row; `listOutcomes()` returns all 7"
    - "`bash scripts/regen-matrix.sh` regenerates all 7 rows sequentially with tmp-dir-then-rename atomicity"
    - "An admission-e2e contract test fails when fixture mtime exceeds 60 days"
    - "An admission-e2e contract test fails if any of the 7 outcome dirs is missing or missing required files"
  artifacts:
    - path: "packages/fixtures/__fixtures__/pr-ready/expectations.ts"
      provides: "Captures the DOG-03 successful run from 10-02 (real-seed trigger)"
      contains: "triggeredBy: 'real-seed'"
    - path: "packages/fixtures/__fixtures__/ambiguous/expectations.ts"
      provides: "Trips 0.2 ambiguity gate (synthetic-intent trigger)"
      contains: "refusalKind"
    - path: "scripts/regen-matrix.sh"
      provides: "Operator-driven regeneration with tmp-dir-then-rename atomicity"
    - path: "packages/admission-e2e/src/fixture-matrix-age.contract.test.ts"
      provides: "60-day staleness gate"
  key_links:
    - from: "packages/admission-e2e tests"
      to: "packages/fixtures/__fixtures__/"
      via: "import expectations + read JSON"
      pattern: "__fixtures__"
---

<objective>
Capture the 7-outcome fixture matrix at `packages/fixtures/__fixtures__/&lt;outcome&gt;/` (hybrid TS-assertion + manifest.json + review-gate.json per Q-06), build the typed accessors (`getMatrixRow`, `listOutcomes`), author `scripts/regen-matrix.sh` regeneration script (operator runs as `bash scripts/regen-matrix.sh`) with atomic writes, and add admission-e2e contract tests for matrix coverage + 60-day age check.

Purpose: DOG-02. The matrix is the canonical "this is what each factory outcome looks like" evidence set; subsequent contract tests assert against it.
Output: 7 fixture rows committed; regen script; coverage + age contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md
@.planning/phases/10-v1-hardening-dogfood/10-02-PLAN.md
@packages/fixtures/src/seeds/index.ts
@packages/admission-e2e/src/_helpers
</context>

<tasks>

<task type="auto">
  <name>Task 0 (pre-flight): Confirm `protostar-factory run` supports envelope tweaks required by Q-07</name>
  <read_first>
    - apps/factory-cli/src/commands/run.ts (Phase 9 surface)
    - packages/intent/src/capability-envelope.ts (envelope shape)
    - .planning/phases/09-operator-surface-resumability/09-CONTEXT.md (CLI surface lock)
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-07 trigger-per-row table)
  </read_first>
  <files>(read-only — no writes)</files>
  <action>
    Q-07 requires the `failed-execution` and `repaired-execution` rows to be triggered by an "envelope tweak" — typically a `writeBudget` override below the natural patch size, or a constrained tool allowlist. Confirm that Phase 9's `protostar-factory run` actually exposes a flag (or environment variable / config-file path) that lets the operator inject capability-envelope overrides at invocation time.

    Verification path:
    1. `node apps/factory-cli/dist/main.js run --help` — inspect for an envelope-related flag (e.g., `--write-budget`, `--envelope-overrides`, `--max-write-bytes`, or similar).
    2. If absent, grep `apps/factory-cli/src/commands/run.ts` and `packages/intent/src/` for any path that loads envelope overrides from a file (e.g., `PROTOSTAR_ENVELOPE_FILE` env var).
    3. **Outcome A — surface exists:** Document the exact flag/path in this Task's notes; Task 2's `failed-execution` and `repaired-execution` recipes will use it.
    4. **Outcome B — surface does NOT exist:** HALT. Do NOT proceed to Task 2's envelope-tweak rows. Surface as a Phase 9 gap-closure dependency: this plan blocks until Phase 9 is amended (gap-closure plan) to expose envelope overrides on `run`. Mocking the failure path with synthetic-intent would silently violate Q-07's trigger-per-row table; that is forbidden.
  </action>
  <acceptance_criteria>
    - Either the envelope-override surface is documented in this Task's evidence notes, OR this plan halts with an explicit Phase 9 gap-closure recommendation
    - No silent fallback to synthetic-intent for envelope-tweak rows
  </acceptance_criteria>
  <verify>
    <automated>node apps/factory-cli/dist/main.js run --help | grep -qE "(write-budget|envelope|max-write)" || grep -rqE "(PROTOSTAR_ENVELOPE|envelopeOverrides|loadEnvelopeOverrides)" apps/factory-cli/src packages/intent/src</automated>
  </verify>
  <done>Envelope-tweak surface confirmed (Outcome A) OR plan halts pending Phase 9 gap-closure (Outcome B)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add `getMatrixRow` accessors + outcome union (TDD)</name>
  <read_first>
    - packages/fixtures/src/index.ts (current barrel from 10-02)
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-06 hybrid format; outcome list)
  </read_first>
  <files>
    packages/fixtures/src/matrix/index.ts,
    packages/fixtures/src/matrix/get-matrix-row.ts,
    packages/fixtures/src/matrix/matrix.test.ts,
    packages/fixtures/src/index.ts
  </files>
  <behavior>
    - Test 1: `listOutcomes()` returns frozen array of all 7 strings: `['accepted','ambiguous','bad-plan','failed-execution','repaired-execution','blocked-review','pr-ready']`
    - Test 2: `getMatrixRow('pr-ready')` returns object with `outcome`, `archetype: 'cosmetic-tweak'`, `expected: { manifestStatus, reviewVerdict, hasPrUrl: true }`, `triggeredBy: 'real-seed'`
    - Test 3: `getMatrixRow('ambiguous')` has `triggeredBy: 'synthetic-intent'` and `expected.refusalKind` defined
    - Test 4: `getMatrixRow('failed-execution')` has `triggeredBy: 'envelope-tweak'`
    - Test 5: `getMatrixRow('unknown')` throws TypeError (compile-time would be ideal, but runtime guard for callers from JS)
    - Test 6: For every outcome, the row's filesystem dir has `expectations.ts` + `manifest.json` (review-gate.json optional per outcome)
  </behavior>
  <action>
    1. Create `Outcome` literal union type: `'accepted' | 'ambiguous' | 'bad-plan' | 'failed-execution' | 'repaired-execution' | 'blocked-review' | 'pr-ready'`.
    2. Create `MatrixRow` interface: `{ outcome: Outcome; archetype: 'cosmetic-tweak'; triggeredBy: 'synthetic-intent' | 'envelope-tweak' | 'real-seed'; expected: { manifestStatus: string; reviewVerdict?: string; hasPrUrl?: boolean; refusalKind?: string } }`.
    3. `listOutcomes()` returns frozen 7-element array.
    4. `getMatrixRow(outcome)` dynamically imports `__fixtures__/&lt;outcome&gt;/expectations.ts` and returns it. Use synchronous `require`-style or top-level static imports indexed by outcome. Throws `Error("unknown outcome: &lt;x&gt;")` on miss.
    5. Re-export from `src/index.ts` barrel.
    6. Author the test file with the 6 behaviors. Test 6 walks the filesystem under `packages/fixtures/__fixtures__/` and asserts each outcome dir + required files exist.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @protostar/fixtures test` passes (6 new matrix tests + 6 seed tests from 10-02 = 12 minimum)
    - `grep -c "'pr-ready'" packages/fixtures/src/matrix/index.ts` &gt;= 1
    - `pnpm run verify` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @protostar/fixtures build &amp;&amp; pnpm --filter @protostar/fixtures test &amp;&amp; pnpm run verify</automated>
  </verify>
  <done>Matrix accessors typed and tested; barrel updated</done>
</task>

<task type="auto">
  <name>Task 2: Author 7 fixture rows + regen script</name>
  <read_first>
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-06 row layout; Q-07 trigger-per-row table; Q-08 regen cadence + atomicity)
    - .planning/phases/10-v1-hardening-dogfood/10-02-EVIDENCE/dog-03-pr-url.txt (use the DOG-03 run as `pr-ready` row source)
    - packages/artifacts/src/manifest-schema.ts (manifest shape)
    - packages/review/src/review-gate-schema.ts (review-gate shape)
    - .protostar/runs/ (pick the DOG-03 run bundle from 10-02 as the seed for `pr-ready`)
  </read_first>
  <files>
    packages/fixtures/__fixtures__/accepted/{expectations.ts,manifest.json,review-gate.json},
    packages/fixtures/__fixtures__/ambiguous/{expectations.ts,manifest.json},
    packages/fixtures/__fixtures__/bad-plan/{expectations.ts,manifest.json},
    packages/fixtures/__fixtures__/failed-execution/{expectations.ts,manifest.json},
    packages/fixtures/__fixtures__/repaired-execution/{expectations.ts,manifest.json,review-gate.json},
    packages/fixtures/__fixtures__/blocked-review/{expectations.ts,manifest.json,review-gate.json},
    packages/fixtures/__fixtures__/pr-ready/{expectations.ts,manifest.json,review-gate.json},
    scripts/regen-matrix.sh,
    package.json
  </files>
  <action>
    1. For the `pr-ready` row, copy `manifest.json` and `review-gate.json` BYTES from the DOG-03 run bundle captured in 10-02 (`.protostar/runs/&lt;runId&gt;/manifest.json` + `review-gate.json`). Write `expectations.ts`:
       ```ts
       export const expectation = {
         outcome: 'pr-ready' as const,
         archetype: 'cosmetic-tweak' as const,
         triggeredBy: 'real-seed' as const,
         expected: {
           manifestStatus: 'delivered',
           reviewVerdict: 'pass',
           hasPrUrl: true,
         },
       } as const;
       ```
    2. For each remaining outcome, **invoke the factory locally** with a tailored intent + capability envelope (Q-07 trigger-per-row table). Capture `manifest.json` (and `review-gate.json` when applicable):
       - `accepted` (real-seed): same as pr-ready but stop before delivery — capture admitted state. `expected: { manifestStatus: 'admitted', reviewVerdict: 'pass' }`. (If no separate "accepted before delivered" state exists in v0.1, document by setting `manifestStatus: 'admitted'` from an early-stage manifest snapshot.)
       - `ambiguous` (synthetic-intent): intent text "do something nice" — trips the `INTENT_AMBIGUITY_THRESHOLD = 0.2` gate. `expected: { manifestStatus: 'refused', refusalKind: 'intent-ambiguous' }`.
       - `bad-plan` (synthetic-intent): intent targeting non-existent file (e.g., "Add a hover state to src/components/DoesNotExist.tsx"). `expected: { manifestStatus: 'refused', refusalKind: 'planning-refusal' }` (exact `refusalKind` per planning admission codes).
       - `failed-execution` (envelope-tweak): real seed + capability envelope `writeBudget` set well below patch size. `expected: { manifestStatus: 'failed', reviewVerdict: 'block' }` (or per actual exec outcome shape).
       - `repaired-execution` (envelope-tweak): envelope tweak that's recoverable in one repair iteration. `expected: { manifestStatus: 'delivered', reviewVerdict: 'pass', repairIterations: 1 }`.
       - `blocked-review` (synthetic-intent): intent that produces a known-bad diff (deletes a file the seed asked to modify). `expected: { manifestStatus: 'review-blocked', reviewVerdict: 'block' }`.
    3. Each `expectations.ts` documents its `triggeredBy` field inline (Q-07 explicit).
    4. Create `scripts/regen-matrix.sh`:
       ```bash
       #!/usr/bin/env bash
       set -euo pipefail
       OUTCOMES=(accepted ambiguous bad-plan failed-execution repaired-execution blocked-review pr-ready)
       FIXTURES_DIR="packages/fixtures/__fixtures__"
       for outcome in "${OUTCOMES[@]}"; do
         tmp_dir="$(mktemp -d)"
         echo "regen: $outcome -> $tmp_dir" 1>&amp;2
         # Operator: invoke the factory for $outcome, write artifacts under $tmp_dir, then atomic rename.
         # Each outcome's recipe lives next to its expectations.ts as a comment block.
         # On full success of $outcome, atomically replace:
         rm -rf "$FIXTURES_DIR/$outcome.tmp"
         mv "$tmp_dir" "$FIXTURES_DIR/$outcome.tmp"
         rm -rf "$FIXTURES_DIR/$outcome"
         mv "$FIXTURES_DIR/$outcome.tmp" "$FIXTURES_DIR/$outcome"
       done
       echo "regen-matrix: done; review diff before commit" 1>&amp;2
       ```
       (The factory invocation per outcome may be implemented inline as a `case` block calling `pnpm run factory` with per-outcome args; keep deterministic.)
       Make executable: `chmod +x scripts/regen-matrix.sh`.
    5. Commit all 7 dirs + script.
  </action>
  <acceptance_criteria>
    - All 7 dirs exist under `packages/fixtures/__fixtures__/`: `for o in accepted ambiguous bad-plan failed-execution repaired-execution blocked-review pr-ready; do test -d packages/fixtures/__fixtures__/$o; done`
    - Every outcome has `expectations.ts` + `manifest.json`: `for o in ...; do test -f packages/fixtures/__fixtures__/$o/expectations.ts &amp;&amp; test -f packages/fixtures/__fixtures__/$o/manifest.json; done`
    - Outcomes that include review (`accepted`, `repaired-execution`, `blocked-review`, `pr-ready`) have `review-gate.json`
    - Each `expectations.ts` includes a `triggeredBy:` literal: `grep -l "triggeredBy:" packages/fixtures/__fixtures__/*/expectations.ts | wc -l` == 7
    - `[ -x scripts/regen-matrix.sh ]` exits 0
  </acceptance_criteria>
  <verify>
    <automated>for o in accepted ambiguous bad-plan failed-execution repaired-execution blocked-review pr-ready; do test -f packages/fixtures/__fixtures__/$o/expectations.ts &amp;&amp; test -f packages/fixtures/__fixtures__/$o/manifest.json || exit 1; done &amp;&amp; for o in accepted repaired-execution blocked-review pr-ready; do test -f packages/fixtures/__fixtures__/$o/review-gate.json || exit 1; done &amp;&amp; [ "$(grep -l 'triggeredBy:' packages/fixtures/__fixtures__/*/expectations.ts | wc -l | tr -d ' ')" = "7" ] &amp;&amp; [ -x scripts/regen-matrix.sh ]</automated>
  </verify>
  <done>7 rows committed; regen script atomic + executable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add matrix coverage + 60-day age contract tests</name>
  <read_first>
    - packages/admission-e2e/src/_helpers (helper patterns)
    - packages/admission-e2e/src/dogpile-adapter-no-fs.contract.test.ts (template static-walker contract)
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-08 60-day stale flag)
  </read_first>
  <files>
    packages/admission-e2e/src/fixture-matrix-coverage.contract.test.ts,
    packages/admission-e2e/src/fixture-matrix-age.contract.test.ts
  </files>
  <behavior>
    - Coverage Test 1: For each of the 7 outcomes from `listOutcomes()`, the corresponding `__fixtures__/&lt;outcome&gt;/expectations.ts` exists and `getMatrixRow(outcome)` resolves.
    - Coverage Test 2: Each outcome's `manifest.json` parses as JSON.
    - Coverage Test 3: Each row's `expectations.ts` exports a `triggeredBy` from the closed set `{'synthetic-intent','envelope-tweak','real-seed'}`.
    - Age Test 1: Each `expectations.ts` mtime within 60 days of test execution; otherwise test fails with a message naming the stale outcome and its age in days.
  </behavior>
  <action>
    1. Add `@protostar/fixtures` as a workspace dep + tsconfig reference of `@protostar/admission-e2e` (mirror existing patterns from `pile-integration-smoke.contract.test.ts`).
    2. Implement `fixture-matrix-coverage.contract.test.ts` using `node:test` + `node:fs`. Resolve fixture root via `import.meta.url` walk to repo root, then `packages/fixtures/__fixtures__/`.
    3. Implement `fixture-matrix-age.contract.test.ts` with `fs.statSync(...).mtimeMs` per outcome's `expectations.ts`; cap = `60 * 24 * 60 * 60 * 1000` ms.
    4. Run `pnpm --filter @protostar/admission-e2e test` — must pass.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @protostar/admission-e2e test --grep "fixture-matrix"` runs ≥4 cases all passing
    - `grep -c "60" packages/admission-e2e/src/fixture-matrix-age.contract.test.ts` &gt;= 1 (60-day cap encoded)
    - `pnpm run verify` exits 0
    - Test output names every outcome in coverage check (so a missing row produces a useful failure message)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @protostar/admission-e2e build &amp;&amp; pnpm --filter @protostar/admission-e2e test &amp;&amp; pnpm run verify</automated>
  </verify>
  <done>Matrix coverage + age contracts in admission-e2e</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed fixtures → contract tests | Fixtures become load-bearing evidence; tampering with manifest.json must surface in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | Fixture row drift | mitigate | Coverage contract + 60-day age contract surface drift in CI |
| T-10-03-02 | Repudiation | Regen-without-evidence | mitigate | `regen-matrix.sh` writes tmp-dir-then-rename; diff is reviewed before commit (Q-08) |
</threat_model>

<verification>
- 7 fixture dirs present with required files
- Coverage + age contracts pass
- `bash scripts/regen-matrix.sh` is the operator-driven regen path (no root npm script needed)
- `pnpm run verify` green
</verification>

<success_criteria>
- DOG-02 fixture matrix has 7 rows, each with `expectations.ts` + `manifest.json` (+`review-gate.json` where applicable)
- `triggeredBy` field documents Q-07 trigger-per-row choice
- 60-day staleness gate fails CI if fixtures rot
</success_criteria>

<output>
After completion, create `.planning/phases/10-v1-hardening-dogfood/10-03-SUMMARY.md`.
</output>
