---
phase: 10-v1-hardening-dogfood
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../protostar-toy-ttt/package.json
  - ../protostar-toy-ttt/README.md
  - ../protostar-toy-ttt/src-tauri/tauri.conf.json
  - ../protostar-toy-ttt/src/components/PrimaryButton.tsx
  - ../protostar-toy-ttt/src/components/Card.tsx
  - ../protostar-toy-ttt/src/components/NavBar.tsx
  - ../protostar-toy-ttt/src/App.tsx
  - ../protostar-toy-ttt/.github/workflows/ci.yml
  - ../protostar-toy-ttt/.gitignore
autonomous: false
requirements: [DOG-01]
must_haves:
  truths:
    - "Sibling repo `../protostar-toy-ttt/` exists on disk and is a Tauri+React+TypeScript app"
    - "Repo is published as public at github.com/zkeown/protostar-toy-ttt"
    - "Repo CI workflow runs on PR and produces required-checks names suitable for `gh pr checks --watch`"
    - "Repo README discloses 'PRs in this repo are opened by the Protostar factory'"
    - "Three rough-edge components (PrimaryButton no-hover, Card no-shadow, NavBar no-aria) exist as seed targets"
    - "Operator has minted a fine-grained `protostar-dogfood` PAT scoped to that one repo"
  artifacts:
    - path: "../protostar-toy-ttt/.github/workflows/ci.yml"
      provides: "CI workflow that DOG-04 dogfood loop polls via `gh pr checks --watch`"
      contains: "pnpm install --frozen-lockfile"
    - path: "../protostar-toy-ttt/README.md"
      provides: "Disclosure that this is a dogfood repo"
      contains: "PRs in this repo are opened by the Protostar factory"
    - path: "../protostar-toy-ttt/src/components/PrimaryButton.tsx"
      provides: "Seed target for `button-color-hover` cosmetic-tweak seed (DOG-03)"
  key_links:
    - from: "scripts/dogfood.sh (Plan 10-08)"
      to: "../protostar-toy-ttt"
      via: "git clone via repo runtime"
      pattern: "protostar-toy-ttt"
---

<objective>
Stand up the sacrificial sibling toy repo at `../protostar-toy-ttt/` (NOT a submodule) with the three rough-edge cosmetic seed targets, a green CI pipeline DOG-04 can poll, and the public-disclosure README. Mint the dedicated `protostar-dogfood` PAT.

Purpose: DOG-01 is the foundation — every later plan in Phase 10 assumes this repo exists and that CI is configured. Without it, DOG-03 has no target and DOG-04 has no pass criterion.
Output: Public sibling repo with CI green on `main` and a PAT in operator's hands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (CHECKPOINT): Create GitHub repo + mint PAT</name>
  <read_first>
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-02 visibility, PAT scoping note)
  </read_first>
  <what-built>Operator action — Claude cannot create the empty repo nor mint the fine-grained PAT autonomously.</what-built>
  <how-to-verify>
    1. Create empty PUBLIC repo `zkeown/protostar-toy-ttt` (no README/license/gitignore — we push initial commit ourselves).
    2. Mint a **fine-grained** PAT named `protostar-dogfood` with these scopes:
       - Resource owner: zkeown
       - Repository access: ONLY `zkeown/protostar-toy-ttt`
       - Permissions: Contents=Read+Write, Pull requests=Read+Write, Metadata=Read, Actions=Read (for `gh pr checks`)
    3. Export PAT into operator's environment as `PROTOSTAR_DOGFOOD_PAT` (do not commit).
  </how-to-verify>
  <acceptance_criteria>
    - `gh repo view zkeown/protostar-toy-ttt --json name,visibility -q '.visibility'` returns `PUBLIC`
    - `gh auth status` shows authentication active
    - `PROTOSTAR_DOGFOOD_PAT` is set in the shell that will run subsequent tasks (verify with `[ -n "$PROTOSTAR_DOGFOOD_PAT" ]` exits 0)
  </acceptance_criteria>
  <verify>
    <automated>gh repo view zkeown/protostar-toy-ttt --json visibility -q '.visibility' | grep -qx 'PUBLIC' &amp;&amp; [ -n "$PROTOSTAR_DOGFOOD_PAT" ]</automated>
  </verify>
  <resume-signal>Type "approved" when repo exists and PAT is in env</resume-signal>
  <done>Empty public repo at zkeown/protostar-toy-ttt; PAT in env</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Tauri+React+TS app and add rough-edge seed targets</name>
  <read_first>
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-01 rough-edge components — exact list)
  </read_first>
  <files>../protostar-toy-ttt/* (entire scaffolded tree)</files>
  <action>
    Working in the parent of the protostar working tree (i.e., `cd ..`):
    1. Run `pnpm create tauri-app@latest protostar-toy-ttt --template react-ts --manager pnpm --identifier com.zkeown.protostar-toy-ttt -y` (non-interactive; if flags differ on installed version, use the closest equivalent — must produce a Tauri+React+TypeScript scaffold).
    2. After scaffold succeeds, replace/add these files (overwrite if scaffold created similar):
       - `src/components/PrimaryButton.tsx` — exports `PrimaryButton` functional component with hard-coded `style={{ backgroundColor: '#3366cc', color: 'white', padding: '8px 16px', border: 'none' }}` and NO hover handling, NO `:hover` CSS, NO `onMouseEnter`. Renders `<button>` accepting `children` prop.
       - `src/components/Card.tsx` — exports `Card` with `style={{ padding: '16px', background: 'white' }}` (NO `boxShadow`, NO `borderRadius`, NO `border`). Renders `<div>` with `children`.
       - `src/components/NavBar.tsx` — exports `NavBar` with three `<button>` items labeled "Home", "About", "Settings". NO `aria-label`, NO `aria-current`, NO `role`.
       - `src/App.tsx` — imports the three components and renders `<NavBar/>` then `<Card><PrimaryButton>Click me</PrimaryButton></Card>`.
    3. Add `.gitignore` entries: `node_modules/`, `dist/`, `src-tauri/target/`, `.env`, `.env.local`.
    4. Run `pnpm install` inside `../protostar-toy-ttt/` and confirm it succeeds.
    5. Confirm `pnpm tauri info` runs without throwing (sanity).
  </action>
  <acceptance_criteria>
    - `test -d ../protostar-toy-ttt/src-tauri` exits 0
    - `grep -c "backgroundColor" ../protostar-toy-ttt/src/components/PrimaryButton.tsx` &gt;= 1
    - `grep -c "hover\|onMouseEnter\|:hover" ../protostar-toy-ttt/src/components/PrimaryButton.tsx` == 0 (intentional rough edge)
    - `grep -c "boxShadow\|borderRadius" ../protostar-toy-ttt/src/components/Card.tsx` == 0 (intentional rough edge)
    - `grep -c "aria-label\|aria-current\|role=" ../protostar-toy-ttt/src/components/NavBar.tsx` == 0 (intentional rough edge)
    - `cat ../protostar-toy-ttt/package.json | jq -r .name` returns the toy app name (string)
  </acceptance_criteria>
  <verify>
    <automated>cd ../protostar-toy-ttt &amp;&amp; test -d src-tauri &amp;&amp; test -f src/components/PrimaryButton.tsx &amp;&amp; test -f src/components/Card.tsx &amp;&amp; test -f src/components/NavBar.tsx &amp;&amp; [ "$(grep -c 'hover\|:hover\|onMouseEnter' src/components/PrimaryButton.tsx)" = "0" ] &amp;&amp; [ "$(grep -c 'boxShadow\|borderRadius' src/components/Card.tsx)" = "0" ] &amp;&amp; [ "$(grep -c 'aria-label\|role=' src/components/NavBar.tsx)" = "0" ]</automated>
  </verify>
  <done>Scaffold present; three rough-edge components in `src/components/`; `pnpm install` clean</done>
</task>

<task type="auto">
  <name>Task 3: Author README with dogfood disclosure + document rough edges</name>
  <read_first>
    - ../protostar-toy-ttt/README.md (whatever scaffold created, if any)
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-01 note on documenting rough edges; Q-02 disclosure text)
  </read_first>
  <files>../protostar-toy-ttt/README.md</files>
  <action>
    Overwrite `../protostar-toy-ttt/README.md` with this exact structure (verbatim disclosure sentence, rest may be expanded):

    ```
    # protostar-toy-ttt

    Sacrificial Tauri+React+TypeScript demo target for the Protostar factory.

    > **PRs in this repo are opened by the Protostar factory.** This is dogfood, not human work.

    ## Why this exists

    This repo is the target the Protostar factory dogfoods against. It is intentionally
    imperfect so cosmetic-tweak seeds have obvious targets to grab.

    ## Intentional rough edges (do not "fix" by hand)

    - `src/components/PrimaryButton.tsx` — hard-coded background color, no `:hover` state.
    - `src/components/Card.tsx` — no shadow, no border, no rounded corners.
    - `src/components/NavBar.tsx` — buttons missing `aria-label` attributes.

    These are seed fixtures for the factory; PRs from the factory will iterate on them.

    ## How PRs get cleaned up

    Branch-per-run, never merged. Operator runs `gh pr list --state open` periodically and
    closes accumulated PRs in batch (`gh pr close <number>` per PR; or `gh pr list --json number -q '.[].number' | xargs -n1 gh pr close`).

    ## Local development

    pnpm install
    pnpm tauri dev
    ```

    The disclosure line MUST appear verbatim: `**PRs in this repo are opened by the Protostar factory.**`
  </action>
  <acceptance_criteria>
    - `grep -F "**PRs in this repo are opened by the Protostar factory.**" ../protostar-toy-ttt/README.md` exits 0
    - README mentions all three rough-edge files by path
    - `grep -c "PrimaryButton.tsx" ../protostar-toy-ttt/README.md` &gt;= 1
    - `grep -c "Card.tsx" ../protostar-toy-ttt/README.md` &gt;= 1
    - `grep -c "NavBar.tsx" ../protostar-toy-ttt/README.md` &gt;= 1
  </acceptance_criteria>
  <verify>
    <automated>grep -F "**PRs in this repo are opened by the Protostar factory.**" ../protostar-toy-ttt/README.md &amp;&amp; grep -q "PrimaryButton.tsx" ../protostar-toy-ttt/README.md &amp;&amp; grep -q "Card.tsx" ../protostar-toy-ttt/README.md &amp;&amp; grep -q "NavBar.tsx" ../protostar-toy-ttt/README.md</automated>
  </verify>
  <done>README has verbatim disclosure + rough-edge inventory</done>
</task>

<task type="auto">
  <name>Task 4: Add CI workflow + initial push to GitHub</name>
  <read_first>
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-10 CI command spec; required-checks consumed by DOG-04)
    - ../protostar-toy-ttt/package.json (to confirm `test` and `build` scripts exist; if `test` missing, add `"test": "echo no tests yet"` so CI doesn't fail before any seed touches it)
  </read_first>
  <files>../protostar-toy-ttt/.github/workflows/ci.yml</files>
  <action>
    Create `../protostar-toy-ttt/.github/workflows/ci.yml`:

    ```yaml
    name: ci
    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]
    jobs:
      build-and-test:
        name: build-and-test
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: pnpm/action-setup@v4
            with:
              version: 9
          - uses: actions/setup-node@v4
            with:
              node-version: 22
              cache: pnpm
          - name: install system deps for tauri
            run: |
              sudo apt-get update
              sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev
          - run: pnpm install --frozen-lockfile
          - run: pnpm test
          - run: pnpm tauri build --debug --no-bundle
    ```

    Required-checks name (consumed by DOG-04 driver in 10-08): `build-and-test`.

    Then:
    1. `cd ../protostar-toy-ttt && git init -b main && git add . && git commit -m "feat: initial scaffold with intentional rough edges + CI"`
    2. `git remote add origin git@github.com:zkeown/protostar-toy-ttt.git` (or `https://...` per operator preference)
    3. `git push -u origin main`
    4. Wait for CI to run on main; confirm green: `gh run list --repo zkeown/protostar-toy-ttt --limit 1 --json conclusion -q '.[0].conclusion'` should eventually be `success`.
    5. If CI fails (e.g., `pnpm test` returns non-zero because no test script), patch `package.json`'s test script to `"test": "echo no tests yet"`, re-commit, re-push, re-verify.
  </action>
  <acceptance_criteria>
    - `test -f ../protostar-toy-ttt/.github/workflows/ci.yml` exits 0
    - `grep -q "build-and-test" ../protostar-toy-ttt/.github/workflows/ci.yml` (job name DOG-04 will key on)
    - `grep -q "pnpm install --frozen-lockfile" ../protostar-toy-ttt/.github/workflows/ci.yml`
    - `grep -q "pnpm tauri build" ../protostar-toy-ttt/.github/workflows/ci.yml`
    - `gh repo view zkeown/protostar-toy-ttt --json name -q '.name'` returns `protostar-toy-ttt`
    - Most recent CI run on `main` has conclusion `success`: `gh run list --repo zkeown/protostar-toy-ttt --branch main --limit 1 --json conclusion -q '.[0].conclusion'` == `success`
  </acceptance_criteria>
  <verify>
    <automated>test -f ../protostar-toy-ttt/.github/workflows/ci.yml &amp;&amp; grep -q "build-and-test" ../protostar-toy-ttt/.github/workflows/ci.yml &amp;&amp; grep -q "pnpm install --frozen-lockfile" ../protostar-toy-ttt/.github/workflows/ci.yml &amp;&amp; gh repo view zkeown/protostar-toy-ttt --json name -q '.name' | grep -qx protostar-toy-ttt &amp;&amp; [ "$(gh run list --repo zkeown/protostar-toy-ttt --branch main --limit 1 --json conclusion -q '.[0].conclusion')" = "success" ]</automated>
  </verify>
  <done>Initial commit pushed; CI green on main; required-check name `build-and-test` is the lock for DOG-04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → GitHub UI | PAT minting, scope selection — outside automation |
| Local fs → sibling repo | First time we author files outside protostar's working tree |
| Toy repo → public web | Repo is public; commits are visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Spoofing | PAT scope | mitigate | Fine-grained PAT scoped to ONLY `zkeown/protostar-toy-ttt`; never reuse general dev PAT |
| T-10-01-02 | Tampering | CI workflow | accept | Public repo on personal account; blast radius limited to dogfood signal |
| T-10-01-03 | Information disclosure | README | mitigate | Disclosure sentence makes dogfood frame explicit to passers-by |
| T-10-01-04 | Elevation of privilege | PAT in env | mitigate | PAT lives in shell env; never committed (gitignore covers `.env*`); used only by 10-08 dogfood driver |
</threat_model>

<verification>
- All four tasks' automated verify commands pass
- `git -C ../protostar-toy-ttt log --oneline` shows ≥1 commit
- Operator confirms PAT is in env (Task 1 checkpoint signal)
</verification>

<success_criteria>
- `../protostar-toy-ttt/` exists, public on GitHub, CI green on main
- Three rough-edge components present and intentional
- README discloses dogfood frame verbatim
- `protostar-dogfood` PAT in operator env, ready for 10-08 driver
</success_criteria>

<output>
After completion, create `.planning/phases/10-v1-hardening-dogfood/10-01-SUMMARY.md`.
</output>
