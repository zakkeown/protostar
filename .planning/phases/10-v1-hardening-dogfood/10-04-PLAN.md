---
phase: 10-v1-hardening-dogfood
plan: 04
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - README.md
  - docs/run-bundle.md
  - docs/run-bundle.appendix.md
  - docs/cli/run.txt
  - docs/cli/status.txt
  - docs/cli/inspect.txt
  - docs/cli/cancel.txt
  - docs/cli/resume.txt
  - docs/cli/deliver.txt
  - docs/cli/prune.txt
  - docs/cli/root.txt
  - packages/artifacts/package.json
  - packages/artifacts/scripts/gen-schema-appendix.ts
  - packages/admission-e2e/src/cli-help-snapshot-drift.contract.test.ts
  - .planning/PROJECT.md
autonomous: true
requirements: [DOG-05]
must_haves:
  truths:
    - "Root README has run-lifecycle mermaid + per-stage prose with file-path landmarks"
    - "`docs/run-bundle.md` opens with hand-written prose; `docs/run-bundle.appendix.md` is generated from artifacts Zod schemas"
    - "`pnpm --filter @protostar/artifacts gen:schema-appendix` produces the appendix deterministically"
    - "`docs/cli/&lt;cmd&gt;.txt` exists for each Phase 9 subcommand and matches the live `--help` output"
    - "An admission-e2e contract fails CI on `docs/cli/*.txt` drift"
    - "PROJECT.md Constraints lock revision adds `zod-to-json-schema` as a dev-dep"
  artifacts:
    - path: "README.md"
      provides: "Operator-first quickstart + lifecycle mermaid"
      contains: "## Run Lifecycle"
    - path: "docs/run-bundle.appendix.md"
      provides: "Auto-generated Zod schema appendix"
    - path: "docs/cli/run.txt"
      provides: "Committed --help snapshot for `protostar-factory run`"
  key_links:
    - from: "README.md"
      to: "docs/run-bundle.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(docs/run-bundle\\.md\\)"
    - from: "docs/cli/*.txt"
      to: "Phase 9 commander modules"
      via: "snapshot test (Q-15)"
      pattern: "docs/cli"
---

<objective>
Author the root README (operator quickstart + run-lifecycle mermaid + per-stage prose with file-path landmarks), `docs/run-bundle.md` hand-written prose + auto-generated `docs/run-bundle.appendix.md`, and commit `docs/cli/&lt;cmd&gt;.txt` `--help` snapshots for every Phase 9 subcommand. Add a CI drift contract.

Purpose: DOG-05. README is the public face once `@protostar/*` packages publish.
Output: README, run-bundle docs, CLI snapshots, drift contract, lock-revision in PROJECT.md.

Note on dependencies: `depends_on: [10-02]` — depends on 10-02 as Q-22 smoke gate, not artifact dependency. This plan also owns the SINGLE PROJECT.md lock-revision edit covering all three Phase 10 dev-deps (`zod-to-json-schema`, `knip`, `@changesets/cli`); 10-05 and 10-06 forbid editing PROJECT.md and serialize behind this plan via their `depends_on: [..., 10-04]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md
@.planning/phases/09-operator-surface-resumability/09-CONTEXT.md
@AGENTS.md
@packages/artifacts/src
@apps/factory-cli/src/commands
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add zod-to-json-schema dev-dep + appendix generator</name>
  <read_first>
    - packages/artifacts/src/ (every Zod schema file — entry list to enumerate in appendix)
    - packages/artifacts/package.json (current scripts; add `gen:schema-appendix`)
    - .planning/PROJECT.md Constraints section (lock-revision pattern from `isomorphic-git`/`commander`)
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-14 hybrid prose+appendix; lock-revision rule)
  </read_first>
  <files>
    packages/artifacts/package.json,
    packages/artifacts/scripts/gen-schema-appendix.ts,
    docs/run-bundle.appendix.md,
    .planning/PROJECT.md
  </files>
  <action>
    1. Add `zod-to-json-schema@^3` as a devDependency on `packages/artifacts/package.json`. Run `pnpm install`.
    2. Create `packages/artifacts/scripts/gen-schema-appendix.ts`:
       - Imports every named Zod schema export from `packages/artifacts/src/index.ts` (and re-exported submodules).
       - For each schema, calls `zodToJsonSchema(schema, schemaName)` and renders a markdown section: `## &lt;schemaName&gt;\n\n```json\n&lt;json-schema&gt;\n```\n\n`.
       - Sorts schema sections alphabetically by name (deterministic output).
       - Writes the entire concatenation to `docs/run-bundle.appendix.md` with header line `&lt;!-- AUTOGENERATED by pnpm --filter @protostar/artifacts gen:schema-appendix; do not edit by hand --&gt;`.
    3. Add npm script to `packages/artifacts/package.json`: `"gen:schema-appendix": "tsx scripts/gen-schema-appendix.ts"` (or compile-and-run via `node` if the project does not use tsx — match existing artifacts pattern; if no tsx, compile under `dist-scripts/` then `node dist-scripts/gen-schema-appendix.js`).
    4. Run the script once to produce the initial `docs/run-bundle.appendix.md`. Commit the result.
    5. Amend `.planning/PROJECT.md` Constraints — add ONE bullet covering ALL Phase 10 dev-dep additions (10-04/10-05/10-06 do not duplicate this edit; this plan owns the narrative):
       `- Phase 10 dev-dep additions (lock revision 2026-04-28; dev-only, not runtime deps): \`zod-to-json-schema@^3\` on \`@protostar/artifacts\` (DOG-05 schema appendix generator), \`knip@^5\` on root (DOG-06 unused-export/dep enforcement), \`@changesets/cli@^2\` on root (DOG-07 release tooling).`
  </action>
  <acceptance_criteria>
    - `pnpm --filter @protostar/artifacts gen:schema-appendix` exits 0
    - `test -s docs/run-bundle.appendix.md` exits 0
    - First line of appendix contains `AUTOGENERATED`
    - `grep -q "zod-to-json-schema" packages/artifacts/package.json`
    - `grep -q "zod-to-json-schema" .planning/PROJECT.md`
    - Re-running `gen:schema-appendix` produces byte-identical output (deterministic): `pnpm --filter @protostar/artifacts gen:schema-appendix &amp;&amp; git diff --exit-code docs/run-bundle.appendix.md`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @protostar/artifacts gen:schema-appendix &amp;&amp; test -s docs/run-bundle.appendix.md &amp;&amp; head -1 docs/run-bundle.appendix.md | grep -q AUTOGENERATED &amp;&amp; grep -q "zod-to-json-schema" packages/artifacts/package.json &amp;&amp; grep -q "zod-to-json-schema" .planning/PROJECT.md &amp;&amp; pnpm --filter @protostar/artifacts gen:schema-appendix &amp;&amp; git diff --exit-code docs/run-bundle.appendix.md</automated>
  </verify>
  <done>Generator wired; appendix committed; PROJECT.md lock-revised; deterministic</done>
</task>

<task type="auto">
  <name>Task 2: Author root README + docs/run-bundle.md prose</name>
  <read_first>
    - README.md (current, may be skeletal)
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-13 surface; Q-16 mermaid + per-stage prose)
    - apps/factory-cli/src/commands (subcommand inventory)
    - packages/intent/src/ambiguity-scoring.ts (file-path landmark)
    - packages/policy/src/admission-paths.ts (archetype routing landmark)
  </read_first>
  <files>README.md, docs/run-bundle.md</files>
  <action>
    1. Replace root `README.md` with this structure:
       - `# Protostar Factory` + one-line value prop
       - `## Quickstart` — operator-first: prereqs, `pnpm install`, `pnpm run verify`, `pnpm run factory -- run --intent "..." --target-repo ...`
       - `## Run Lifecycle` — Mermaid sequence diagram showing: `intent → admission → planning → execution → review → repair? → evaluation → evolution → delivery`. Each lane labeled by package.
       - For each lifecycle stage, a `### &lt;stage&gt;` subsection with at least ONE full file path (e.g., `Planning: ambiguity gate at \`packages/intent/src/ambiguity-scoring.ts\` (threshold 0.2); refusals append to \`.protostar/refusals.jsonl\``).
       - `## CLI Reference` — link to `docs/cli/` directory; one bullet per subcommand naming the snapshot file.
       - `## Run Bundle` — link to `docs/run-bundle.md` and `docs/run-bundle.appendix.md`.
       - `## Packages` — link to per-package READMEs (DOG-06 lands these in 10-05; this section can list them with relative links that resolve once 10-05 lands; CI does not yet enforce link validity).
       - `## Security` — link to `SECURITY.md` (10-07 creates).
    2. Author `docs/run-bundle.md`:
       - "Why each artifact exists" prose covering `manifest.json`, `intent.json`, `plan.json`, `execution-plan.json`, `review-gate.json`, `evolution/snapshot.json`, `piles/&lt;kind&gt;/iter-&lt;N&gt;/`.
       - Closing line: `For machine-readable JSON Schema, see [run-bundle.appendix.md](./run-bundle.appendix.md).`
    3. Mermaid block syntax check by GitHub: ensure block opens with ` ```mermaid` and closes with ` ``` `.
  </action>
  <acceptance_criteria>
    - `grep -q "## Run Lifecycle" README.md`
    - `grep -c '```mermaid' README.md` &gt;= 1
    - For each stage subsection, at least one full file path is present: `grep -E "(packages|apps)/[a-z-]+/src/" README.md | wc -l` &gt;= 5
    - `grep -q "docs/run-bundle.md" README.md`
    - `grep -q "docs/cli" README.md`
    - `grep -q "run-bundle.appendix.md" docs/run-bundle.md`
    - `test -s docs/run-bundle.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "## Run Lifecycle" README.md &amp;&amp; grep -q '```mermaid' README.md &amp;&amp; [ "$(grep -cE '(packages|apps)/[a-z-]+/src/' README.md)" -ge 5 ] &amp;&amp; grep -q "docs/run-bundle.md" README.md &amp;&amp; grep -q "docs/cli" README.md &amp;&amp; grep -q "run-bundle.appendix.md" docs/run-bundle.md</automated>
  </verify>
  <done>README operator-ready with mermaid + landmarks; run-bundle prose links appendix</done>
</task>

<task type="auto">
  <name>Task 3: Capture CLI --help snapshots + drift contract</name>
  <read_first>
    - apps/factory-cli/src/commands/ (every command exposes commander Command via Phase 9)
    - packages/admission-e2e/src/_helpers (snapshot helper patterns; Phase 9 may already have `--help` capture tests)
    - .planning/phases/10-v1-hardening-dogfood/10-CONTEXT.md (Q-15: snapshot tests write `docs/cli/*.txt` when `UPDATE_FIXTURES=1`; CI fails on drift)
  </read_first>
  <files>
    docs/cli/root.txt,
    docs/cli/run.txt,
    docs/cli/status.txt,
    docs/cli/inspect.txt,
    docs/cli/cancel.txt,
    docs/cli/resume.txt,
    docs/cli/deliver.txt,
    docs/cli/prune.txt,
    packages/admission-e2e/src/cli-help-snapshot-drift.contract.test.ts
  </files>
  <action>
    1. Capture initial snapshots by invoking the built CLI:
       ```
       mkdir -p docs/cli
       node apps/factory-cli/dist/main.js --help &gt; docs/cli/root.txt
       for cmd in run status inspect cancel resume deliver prune; do
         node apps/factory-cli/dist/main.js "$cmd" --help &gt; "docs/cli/$cmd.txt"
       done
       ```
    2. Strip non-deterministic content from each snapshot file (e.g., absolute paths, version strings if they include git SHAs). Preferred: a small post-processor script that normalizes the help output deterministically. If commander's output is already deterministic (it generally is — flag order, descriptions), skip.
    3. Add `packages/admission-e2e/src/cli-help-snapshot-drift.contract.test.ts`:
       - **Derive the snapshot set at test time by walking the commander tree** rather than enumerating commands inline. Strategy: import the `program` builder from `apps/factory-cli/dist/main.js` (or invoke `node apps/factory-cli/dist/main.js --help` and parse the "Commands:" section from stdout — both routes are acceptable; commander tree introspection is preferred).
       - Filter out any command whose name starts with `__` (double-underscore prefix marks internal-only subcommands; these are NOT public surface and MUST NOT have a `docs/cli/*.txt` snapshot — see 10-08's `__dogfood-step` for an example).
       - For each remaining command (including the root program):
         - Spawn `node apps/factory-cli/dist/main.js [cmd] --help` (root has no cmd token).
         - Read `docs/cli/&lt;cmd&gt;.txt`.
         - Assert byte-equality.
       - **Coverage assertion:** every non-`__`-prefixed command MUST have a corresponding `docs/cli/&lt;name&gt;.txt`. A new public command added without a snapshot fails this contract. Conversely, a `docs/cli/&lt;name&gt;.txt` with no matching command fails too.
       - When env `UPDATE_FIXTURES=1`, write the fresh output back to `docs/cli/&lt;cmd&gt;.txt` instead of asserting (Q-15). Under `UPDATE_FIXTURES=1`, also DELETE any orphan `docs/cli/*.txt` whose command no longer exists in the commander tree.
    4. Add `@protostar/factory-cli` as a workspace dep (if not already) on `@protostar/admission-e2e` for the test to spawn the built binary path.
    5. Run the test once with snapshots committed; verify no drift. Confirm the `__dogfood-step` (added by 10-08) is correctly excluded from the snapshot set.
  </action>
  <acceptance_criteria>
    - 8 files exist: `for f in root run status inspect cancel resume deliver prune; do test -s docs/cli/$f.txt || exit 1; done`
    - Each snapshot is non-empty (smoke for capture step)
    - `pnpm --filter @protostar/admission-e2e test --grep "cli-help-snapshot-drift"` passes
    - Test references env var `UPDATE_FIXTURES`: `grep -q "UPDATE_FIXTURES" packages/admission-e2e/src/cli-help-snapshot-drift.contract.test.ts`
    - Running it twice in a row stays green: `pnpm --filter @protostar/admission-e2e test --grep "cli-help-snapshot-drift" &amp;&amp; pnpm --filter @protostar/admission-e2e test --grep "cli-help-snapshot-drift"`
  </acceptance_criteria>
  <verify>
    <automated>for f in root run status inspect cancel resume deliver prune; do test -s docs/cli/$f.txt || exit 1; done &amp;&amp; grep -q "UPDATE_FIXTURES" packages/admission-e2e/src/cli-help-snapshot-drift.contract.test.ts &amp;&amp; pnpm --filter @protostar/admission-e2e build &amp;&amp; pnpm --filter @protostar/admission-e2e test &amp;&amp; pnpm run verify</automated>
  </verify>
  <done>CLI snapshots committed; drift contract green; `UPDATE_FIXTURES=1` is the regen toggle</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | CLI snapshot drift | mitigate | Drift contract test (Task 3) fails CI; `UPDATE_FIXTURES=1` is the explicit regen path |
| T-10-04-02 | Repudiation | Schema appendix drift | mitigate | Generator is deterministic; CI re-runs and `git diff --exit-code` would catch drift in 10-08 verify wave |
</threat_model>

<verification>
- Schema appendix generated and deterministic
- README has lifecycle mermaid + per-stage file paths
- 8 CLI snapshots locked + drift contract passing
- PROJECT.md lock-revision lists `zod-to-json-schema`
</verification>

<success_criteria>
- DOG-05 deliverables: README run lifecycle accurate; CLI reference complete; run-bundle schema documented hybrid prose+appendix
</success_criteria>

<output>
After completion, create `.planning/phases/10-v1-hardening-dogfood/10-04-SUMMARY.md`.
</output>
