---
phase: 04-execution-engine
plan: 07
subsystem: intent
tags: [confirmed-intent, capability-envelope, schema, signatures, admission-e2e]

requires:
  - phase: 04-execution-engine
    provides: execution contracts and signed intent verification paths from Plan 02
provides:
  - ConfirmedIntent schemaVersion 1.3.0 hard bump
  - Required network and execution budget envelope fields
  - Signed 1.3.0 round-trip admission coverage
affects: [intent, authority, admission-e2e, factory-cli, lmstudio-adapter]

tech-stack:
  added: []
  patterns: [schema const hard-bump, hand-written parser enforcement, signed fixture verification]

key-files:
  created:
    - packages/admission-e2e/src/signed-intent-1-3-0.test.ts
  modified:
    - packages/intent/schema/confirmed-intent.schema.json
    - packages/intent/src/capability-envelope.ts
    - packages/intent/src/confirmed-intent.ts
    - packages/intent/src/capability-normalization.ts
    - packages/authority/src/stage-reader/factory.ts
    - apps/factory-cli/src/main.test.ts

key-decisions:
  - "ConfirmedIntent 1.3.0 is a hard parser/schema bump, not a compatibility union."
  - "Generated signed-intent tests build signatures in code; no standalone signed JSON regeneration script was needed."
  - "Legacy budget cap fields remain accepted alongside the new required execution budget knobs."

patterns-established:
  - "Capability envelope defaults are normalized at draft promotion boundaries, while parseConfirmedIntent enforces explicit 1.3.0 fields for external JSON."
  - "Trusted-launch tests sign against the same repo-policy and precedence-resolved envelope snapshot used by factory execution."

requirements-completed: [EXEC-06, EXEC-07]

duration: 1h 45m
completed: 2026-04-27T23:08:38Z
---

# Phase 04 Plan 07: Envelope Schema Bump Summary

**ConfirmedIntent 1.3.0 hard bump with explicit network and execution budget envelope fields, plus signed round-trip verification.**

## Performance

- **Duration:** 1h 45m
- **Started:** 2026-04-27T21:23:00Z
- **Completed:** 2026-04-27T23:08:38Z
- **Tasks:** 2
- **Files modified:** 24

## Accomplishments

- Bumped `confirmed-intent.schema.json` to a single `"1.3.0"` const with required `network.allow`, allowlist `allowedHosts` if/then enforcement, and required `adapterRetriesPerTask`, `taskWallClockMs`, and `maxRepairLoops` budget fields.
- Updated the TypeScript capability envelope model, parser validation, normalization defaults, and intent fixtures/tests to match the 1.3.0 schema.
- Added admission e2e coverage that builds, signs, verifies, and reads a 1.3.0 ConfirmedIntent containing the new fields.
- Swept downstream authority, factory-cli, and LM Studio fixture/test references so no non-dist source or fixture still pins `"1.2.0"`.

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Bump schema + TS types + envelope tests** - `e13afe6` (test)
2. **Task 1 GREEN: Bump schema + TS types + envelope tests** - `29bce05` (feat)
3. **Task 2: Regenerate signed-intent fixtures + repo-wide verify sweep** - `468d98d` (fix)

**Plan metadata:** this summary file is committed separately after self-check.

## Files Created/Modified

- `packages/intent/schema/confirmed-intent.schema.json` - Pins schema version to 1.3.0 and defines the new network/budget envelope contract.
- `packages/intent/src/capability-envelope.ts` - Mirrors the 1.3.0 capability envelope shape in TypeScript.
- `packages/intent/src/confirmed-intent.ts` - Enforces explicit 1.3.0 capability envelope fields at parse time.
- `packages/intent/src/capability-normalization.ts` - Adds v0.1 defaults for loopback network and execution budget knobs.
- `packages/intent/src/capability-envelope.test.ts` - Covers schema hard-bump, allowlist validation, budget minimums, and TS assignability.
- `packages/admission-e2e/src/signed-intent-1-3-0.test.ts` - Builds, signs, verifies, and inspects a 1.3.0 ConfirmedIntent.
- `packages/authority/src/stage-reader/factory.ts` - Upconverts legacy diagnostic stage fixtures into explicit 1.3.0 envelopes.
- `apps/factory-cli/src/main.test.ts` - Signs trusted-launch fixtures with the same resolved envelope and policy snapshot used by execution.
- `packages/lmstudio-adapter/internal/test-fixtures/cosmetic-tweak-fixture.ts` - Removes the stale 1.2.0 fixture pin.

## Decisions Made

- Kept the schema and parser as hard 1.3.0 gates. No union schema, no 1.2.0 compatibility parser path.
- Did not add a fixture regeneration script because signed intents in this repo are generated by tests/helpers rather than stored as signed JSON fixture files.
- Preserved legacy budget cap keys (`maxUsd`, `maxTokens`, `timeoutMs`) as optional envelope properties while requiring the new execution budget fields.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Preserved legacy budget cap properties**
- **Found during:** Task 1 (Bump schema + TS types + envelope tests)
- **Issue:** Replacing the budget shape verbatim would drop existing authority cap fields still used by policy/admission tests.
- **Fix:** Required the new execution budget fields while retaining optional `maxUsd`, `maxTokens`, and `timeoutMs`.
- **Files modified:** `packages/intent/schema/confirmed-intent.schema.json`, `packages/intent/src/capability-envelope.ts`, related intent tests.
- **Verification:** `pnpm --filter @protostar/intent test`, `pnpm run verify`.
- **Committed in:** `29bce05`

**2. [Rule 3 - Blocking] Updated authority legacy stage upconversion**
- **Found during:** Task 2 (Repo-wide verify sweep)
- **Issue:** Stage-reader legacy fixtures upgraded from 1.0.0 to 1.2.0 and then failed the new 1.3.0 parser boundary.
- **Fix:** Upconverted legacy stage fixtures to 1.3.0 and added workspace/network/budget defaults.
- **Files modified:** `packages/authority/src/stage-reader/factory.ts`, `packages/authority/src/stage-reader/factory.test.ts`.
- **Verification:** `pnpm --filter @protostar/authority test`, `pnpm run verify`.
- **Committed in:** `468d98d`

**3. [Rule 3 - Blocking] Re-signed trusted-launch test fixtures against resolved policy state**
- **Found during:** Task 2 (Repo-wide verify sweep)
- **Issue:** Factory trusted-launch tests produced envelope/policy hash mismatches after the 1.3.0 defaults changed the canonical signed body.
- **Fix:** Updated the test helper to sign with the repo policy and precedence-resolved envelope that `runFactory` verifies.
- **Files modified:** `apps/factory-cli/src/main.test.ts`, `apps/factory-cli/src/two-key-launch.test.ts`.
- **Verification:** `pnpm --filter @protostar/factory-cli test`, `pnpm run verify`.
- **Committed in:** `468d98d`

**4. [Rule 2 - Missing Critical] Removed stale LM Studio 1.2.0 fixture pin**
- **Found during:** Task 2 (Repo-wide no-1.2.0 sweep)
- **Issue:** A stale LM Studio cosmetic fixture still carried `"schemaVersion": "1.2.0"` outside the original files list, violating the plan acceptance gate.
- **Fix:** Updated the fixture schemaVersion to 1.3.0.
- **Files modified:** `packages/lmstudio-adapter/internal/test-fixtures/cosmetic-tweak-fixture.ts`.
- **Verification:** `rg -n '"schemaVersion": "1\\.2\\.0"|"1\\.2\\.0"' examples packages --glob '!**/dist/**' --glob '!**/node_modules/**'` returned no matches.
- **Committed in:** `468d98d`

---

**Total deviations:** 4 auto-fixed (2 Rule 2, 2 Rule 3)  
**Impact on plan:** All deviations were required to keep the hard 1.3.0 bump correct and repo-wide verification green. No compatibility shim was introduced.

## Verification

- `pnpm --filter @protostar/intent test` - PASS
- `pnpm --filter @protostar/admission-e2e test` - PASS
- `pnpm --filter @protostar/authority test` - PASS
- `pnpm --filter @protostar/factory-cli test` - PASS
- `pnpm run verify` - PASS
- `rg -n '"schemaVersion": "1\\.2\\.0"|"1\\.2\\.0"' examples packages --glob '!**/dist/**' --glob '!**/node_modules/**'` - PASS, no matches

## Known Stubs

None introduced.

## Threat Flags

None beyond the planned schema/signature trust-boundary work. The new surface is mitigated by the hard version const, allowlist `if/then` requirement, parser enforcement, signed round-trip coverage, and repo-wide verify sweep.

## Issues Encountered

- `rg` returns exit code 1 when no stale `1.2.0` matches exist; this was treated as the expected pass condition.
- Parallel executor changes remained present in `.planning/ROADMAP.md`, Phase 5/6 planning files, and `.protostar/refusals.jsonl`; they were not staged or modified by this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Wave 2 plans can now consume ConfirmedIntent 1.3.0 fields and signed-intent verification without 1.2.0 envelope-hash drift.

## Self-Check: PASSED

- Summary file exists: `.planning/phases/04-execution-engine/04-07-envelope-schema-bump-SUMMARY.md`
- Task commits found in git history: `e13afe6`, `29bce05`, `468d98d`
- Stub scan found no TODO/FIXME/placeholder markers in the files created or materially changed by this plan.

---
*Phase: 04-execution-engine*
*Completed: 2026-04-27*
