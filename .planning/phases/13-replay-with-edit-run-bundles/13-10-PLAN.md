---
phase: 13-replay-with-edit-run-bundles
plan: 10
type: execute
wave: 3
depends_on: [13-05, 13-07]
files_modified:
  - packages/replay/src/counterfactual/delta.ts
  - packages/replay/src/counterfactual/delta.test.ts
  - packages/replay/src/index.ts
  - apps/factory-cli/src/replay-counterfactual-writer.ts
  - apps/factory-cli/src/replay-counterfactual-writer.test.ts
  - apps/factory-cli/src/commands/replay.ts
  - apps/factory-cli/src/commands/replay.test.ts
  - packages/evaluation-runner/src/run-evaluation-stages.ts
  - packages/evaluation-runner/src/run-evaluation-stages.test.ts
autonomous: true
requirements: [REPLAY-15]
must_haves:
  truths:
    - "Each replay produces `runs/<parentRunId>/replays/<replayId>/evaluation/counterfactual.json` validating against `CounterfactualReportSchema`"
    - "Counterfactual computes verdict + score delta vs the parent run's evaluation report and tags the cascadedStages"
    - "Phase 8 `decideEvolution` accepts an optional `counterfactual` input and treats it as a soft evolution signal (judge-/reviewer-/planning-bottleneck hints)"
    - "Original-run evolution behavior is unchanged when no counterfactual is provided (back-compat)"
  artifacts:
    - path: "packages/replay/src/counterfactual/delta.ts"
      provides: "computeCounterfactualDelta — pure"
    - path: "apps/factory-cli/src/replay-counterfactual-writer.ts"
      provides: "atomic writer for counterfactual.json"
    - path: "packages/evaluation-runner/src/run-evaluation-stages.ts"
      contains: "counterfactual"
  key_links:
    - from: "apps/factory-cli/src/commands/replay.ts"
      to: "apps/factory-cli/src/replay-counterfactual-writer.ts"
      via: "post-evaluation counterfactual write"
      pattern: "writeCounterfactualReport"
    - from: "packages/evaluation-runner/src/run-evaluation-stages.ts"
      to: "decideEvolution at line 337"
      via: "optional counterfactual input"
      pattern: "counterfactual"
---

<objective>
D-14 wiring: every replay writes `replays/<replayId>/evaluation/counterfactual.json` capturing the eval delta vs the parent run; Phase 8's `decideEvolution` (at `packages/evaluation-runner/src/run-evaluation-stages.ts:337`) is extended to accept an optional `counterfactual` input and treat it as a soft hint when present.

The hint is a soft signal in v1 (Open Question #2 in 13-RESEARCH.md): `evolutionSignal.confidence` is recorded but a single counterfactual cannot flip the evolution decision alone; tuning is empirical, mirroring Phase 8 EVOL-03 calibration.

Purpose: replay's payoff (counterfactual debugging → evolution leverage) only realizes when Phase 8 reads the artifact. Without this plan, counterfactual.json is an orphan artifact.
Output: counterfactual delta computation + writer + Phase 8 ingestion seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-replay-with-edit-run-bundles/13-CONTEXT.md
@.planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md
@packages/replay/src/counterfactual/schema.ts
@packages/evaluation-runner/src/run-evaluation-stages.ts

<interfaces>
<!-- Existing seam (verified) -->

From packages/evaluation-runner/src/run-evaluation-stages.ts:21,337:
```typescript
import { decideEvolution } from "@protostar/evaluation";
// line 337:
return decideEvolution({
  // existing inputs
});
```
Plan extends `decideEvolution` input with optional `counterfactual?: CounterfactualReport`.

From @protostar/replay (plan 13-02):
```typescript
export const CounterfactualReportSchema;
export type CounterfactualReport;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computeCounterfactualDelta — pure delta computation + signal hint</name>
  <files>
    packages/replay/src/counterfactual/delta.ts,
    packages/replay/src/counterfactual/delta.test.ts,
    packages/replay/src/index.ts
  </files>
  <read_first>
    - packages/replay/src/counterfactual/schema.ts (CounterfactualReportSchema — created in plan 13-02)
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (Counterfactual Report code block — evolutionSignal hint enum)
    - .planning/phases/13-replay-with-edit-run-bundles/13-CONTEXT.md (D-14 — soft hint posture; calibration deferred to Phase 8 dogfood)
  </read_first>
  <behavior>
    - Signature: `computeCounterfactualDelta(input: { parentRunId; replayId; parentEvaluation: { verdict; score }; replayEvaluation: { verdict; score }; cascadedStages: ReadonlyArray<string> }): CounterfactualReport`.
    - Returns a `CounterfactualReport`:
      - `delta.verdictChanged = parentEvaluation.verdict !== replayEvaluation.verdict`
      - `delta.scoreDelta = replayEvaluation.score - parentEvaluation.score`
      - `delta.cascadedStages = input.cascadedStages` (copied)
      - `evolutionSignal.hint`: derive deterministically from cascadedStages — if review/* dominates the cascade and verdict changed → `"reviewer-bottleneck"`; if any pile of kind `review` was edited → `"reviewer-bottleneck"`; if cascade is execution-only → `"no-signal"`; if planning-pile is in cascade → `"planning-bottleneck"`; otherwise `"no-signal"`. (Document this rule in code; v1 is intentionally simple.)
      - `evolutionSignal.confidence`: 0.5 by default for non-no-signal hints; 0.0 for `"no-signal"`. Calibration is empirical (Phase 8 dogfood; deferred per Open Question #2).
    - Function is pure — no I/O.
    - Test (RED→GREEN): verdict-flipped + review cascade → reviewer-bottleneck, confidence 0.5.
    - Test: identical verdicts → verdictChanged=false; scoreDelta=0; hint=no-signal, confidence=0.
    - Test: planning-pile in cascade → planning-bottleneck.
    - Test: result schema-validates against `CounterfactualReportSchema`.
  </behavior>
  <action>
    1. RED: write `delta.test.ts` with the four behaviors. Verify failure.
    2. GREEN: implement `delta.ts` with the deterministic hint derivation. Use the schema to validate the output before returning (defense-in-depth).
    3. Export from `packages/replay/src/index.ts`.
    4. `pnpm --filter @protostar/replay test`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; pnpm --filter @protostar/replay build &amp;&amp; pnpm --filter @protostar/replay test &amp;&amp; grep -q 'computeCounterfactualDelta' packages/replay/src/index.ts &amp;&amp; grep -qE 'reviewer-bottleneck|planning-bottleneck' packages/replay/src/counterfactual/delta.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'computeCounterfactualDelta' packages/replay/src/index.ts` succeeds
    - 4 test cases pass
    - Function output validates against `CounterfactualReportSchema` for every test input
    - Pure-tier no-fs/no-net contracts still pass
  </acceptance_criteria>
  <done>Pure delta + hint computation is ready for the writer and Phase 8 ingestion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: counterfactual writer + replay command integration</name>
  <files>
    apps/factory-cli/src/replay-counterfactual-writer.ts,
    apps/factory-cli/src/replay-counterfactual-writer.test.ts,
    apps/factory-cli/src/commands/replay.ts,
    apps/factory-cli/src/commands/replay.test.ts
  </files>
  <read_first>
    - apps/factory-cli/src/replay-paths-read-writer.ts (atomic tmp+rename pattern; mirror it)
    - apps/factory-cli/src/commands/replay.ts (current execute flow from plan 13-07; the integration point is post-cascade, after evaluation has re-run)
    - apps/factory-cli/src/run-evaluation.ts (parent evaluation report path)
  </read_first>
  <behavior>
    - `writeCounterfactualReport({ replayDir, report }): Promise<{ path; sha256 }>` — schema-validates, writes `<replayDir>/evaluation/counterfactual.json` via tmp+rename, byte-stable serialization via `sortJsonValue`.
    - In `commands/replay.ts`, after the cascaded evaluation stages complete (and only if the parent run had an `evaluation-report.json`): load parent eval report, build the replay eval report from cascaded stage outputs, call `computeCounterfactualDelta`, then call `writeCounterfactualReport`.
    - If parent run has no evaluation report, skip silently — counterfactual is meaningful only when both runs evaluated.
    - Test (RED→GREEN): fixture parent run with evaluation report + replay produces a counterfactual file with valid schema and the expected delta values.
    - Test: parent run without evaluation report → no counterfactual file written; replay still completes successfully.
    - Test: byte-stable second write with same inputs.
  </behavior>
  <action>
    1. RED: write `replay-counterfactual-writer.test.ts` and extend `commands/replay.test.ts` with the integration test. Verify failure.
    2. GREEN: implement the writer. Wire it into the replay flow at the post-evaluation seam (after cascaded evaluation stages). Use the per-stage map from plan 13-05 to build replayEvaluation, and the parent's `runs/<parentRunId>/evaluation-report.json` for parentEvaluation.
    3. `pnpm --filter @protostar/factory-cli test && pnpm run verify`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; pnpm --filter @protostar/factory-cli build &amp;&amp; pnpm --filter @protostar/factory-cli test &amp;&amp; pnpm run verify &amp;&amp; grep -q 'writeCounterfactualReport' apps/factory-cli/src/commands/replay.ts &amp;&amp; grep -q 'counterfactual.json' apps/factory-cli/src/replay-counterfactual-writer.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'writeCounterfactualReport' apps/factory-cli/src/commands/replay.ts` succeeds
    - Three new test cases pass
    - Counterfactual file present at the expected path with valid schema after a fixture replay
    - No counterfactual when parent eval report absent (back-compat)
  </acceptance_criteria>
  <done>Each replay produces a counterfactual artifact; Phase 8 can ingest it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: decideEvolution accepts optional counterfactual input</name>
  <files>
    packages/evaluation-runner/src/run-evaluation-stages.ts,
    packages/evaluation-runner/src/run-evaluation-stages.test.ts
  </files>
  <read_first>
    - packages/evaluation-runner/src/run-evaluation-stages.ts (full — line 21 import, line 337 decideEvolution call)
    - packages/evaluation/src/index.ts (export of decideEvolution; signature)
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (Hook Point: Evolution Counterfactual Ingestion code block)
  </read_first>
  <behavior>
    - The function that calls `decideEvolution` at line 337 accepts an optional `counterfactual?: CounterfactualReport` input field. When provided, the field is forwarded to `decideEvolution`.
    - `decideEvolution` in `@protostar/evaluation` accepts the new optional `counterfactual` input. v1 behavior: when present and `evolutionSignal.hint !== "no-signal"`, the existing evolution decision is annotated with a `"counterfactualHint": <hint>` field on the result. **The decision is NOT changed by the hint in v1** (soft signal posture per Open Question #2).
    - Test (RED→GREEN): without counterfactual → existing behavior unchanged (back-compat assertion against an existing fixture).
    - Test: with counterfactual `hint: "reviewer-bottleneck"` → decision result carries the annotation; the decision verdict is still computed by the existing logic (e.g. continue/converged/exhausted).
    - Test: `hint: "no-signal"` → no annotation added (or annotation explicitly empty).
  </behavior>
  <action>
    1. RED: extend `run-evaluation-stages.test.ts` with the three behaviors. Verify failure.
    2. GREEN: thread the optional input through `runEvaluationStages` (or whichever caller-facing function) and into `decideEvolution`. Update `decideEvolution`'s signature in `@protostar/evaluation` to accept the optional input and emit the annotation.
    3. `pnpm --filter @protostar/evaluation test && pnpm --filter @protostar/evaluation-runner test && pnpm run verify`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; pnpm --filter @protostar/evaluation build &amp;&amp; pnpm --filter @protostar/evaluation test &amp;&amp; pnpm --filter @protostar/evaluation-runner build &amp;&amp; pnpm --filter @protostar/evaluation-runner test &amp;&amp; pnpm run verify &amp;&amp; grep -q 'counterfactual' packages/evaluation-runner/src/run-evaluation-stages.ts &amp;&amp; grep -q 'counterfactual' packages/evaluation/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'counterfactual' packages/evaluation-runner/src/run-evaluation-stages.ts` succeeds
    - `grep -q 'counterfactual' packages/evaluation/src/index.ts` succeeds
    - Three new test cases pass (no-counterfactual back-compat, reviewer-bottleneck annotation, no-signal no-op)
    - Existing Phase 8 evolution tests green (no regression on `decideEvolution` without counterfactual)
    - `pnpm run verify` green
  </acceptance_criteria>
  <done>Phase 8 ingests counterfactual hints as soft signals; replay's evolution-leverage payoff is realized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay-produced counterfactual.json → Phase 8 evolution | counterfactual is operator-influenced (via edit) and influences evolution decisions in v2; in v1 it's a soft annotation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10-01 | Tampering | malicious counterfactual flips evolution decision | accept | v1 hint is annotation-only (does not change verdict); empirical calibration deferred to Phase 8 dogfood per Open Question #2 |
| T-13-10-02 | Tampering | counterfactual.json schema-malformed | mitigate | CounterfactualReportSchema validates on write (Task 2) and on Phase 8 ingest (Task 3) |
| T-13-10-03 | Spoofing | counterfactual references wrong parentRunId | mitigate | Writer ensures `parentRunId`/`replayId` match the replay bundle context; Phase 8 ingestion checks the IDs against the run being evaluated |
</threat_model>

<verification>
- `pnpm run verify` green
- Fixture replay produces `replays/<replayId>/evaluation/counterfactual.json` schema-validated
- Phase 8 evolution behavior unchanged when no counterfactual present
- Phase 8 evolution decision carries `counterfactualHint` annotation when counterfactual present and hint != no-signal
</verification>

<success_criteria>
- REPLAY-15 (counterfactual.json + decideEvolution hookup): satisfied
- v1 soft-hint posture preserved (no decision flips on a single counterfactual)
- Path is open for Phase 8 dogfood empirical calibration
</success_criteria>

<output>
After completion, create `.planning/phases/13-replay-with-edit-run-bundles/13-10-SUMMARY.md`
</output>
