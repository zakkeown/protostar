---
phase: 13-replay-with-edit-run-bundles
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md
  - .planning/phases/13-replay-with-edit-run-bundles/13-PHASE12-WIRING-PRECONDITION.md
autonomous: true
requirements: [REPLAY-01, REPLAY-02, REPLAY-03, REPLAY-04, REPLAY-05, REPLAY-06, REPLAY-07, REPLAY-08, REPLAY-09, REPLAY-10, REPLAY-11, REPLAY-12, REPLAY-13, REPLAY-14, REPLAY-15, REPLAY-16, REPLAY-17]
must_haves:
  truths:
    - "REPLAY-01..REPLAY-17 are committed to .planning/REQUIREMENTS.md and traceable per phase"
    - "ADR pinning the read-set sidecar surface (option c, uniform <stage>/pathsRead.json) is committed"
    - "Phase 12 plan 12-06 (wiring extraction) merge precondition is recorded"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      contains: "REPLAY-01"
    - path: ".planning/REQUIREMENTS.md"
      contains: "REPLAY-17"
    - path: ".planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md"
      provides: "ADR pinning sidecar choice and rationale"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 13 plan-set requirements frontmatter"
      via: "REPLAY-NN IDs referenced by every Phase 13 plan"
      pattern: "REPLAY-[0-9]{2}"
---

<objective>
Commit REPLAY-01..REPLAY-17 to `.planning/REQUIREMENTS.md` with the same shape used for Phase 11/12 (status `Pending`, traceability rows). Pin the read-set instrumentation surface (uniform sidecar — option c) in an ADR so downstream plans implement against a fixed contract. Record the Phase 12 plan 12-06 wiring-extraction precondition (Pitfall #6 in 13-RESEARCH.md): subsequent waves must verify 12-06 has merged before they touch `apps/factory-cli/src/wiring/` or `main.ts`.

Purpose: every other Phase 13 plan must reference REPLAY-NN IDs in its `requirements` frontmatter and write code against a fixed read-set surface. Without this plan, the planner constraint "every requirement ID must appear in at least one plan" is unverifiable.
Output: REQUIREMENTS.md update + 2 ADR/precondition docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/13-replay-with-edit-run-bundles/13-CONTEXT.md
@.planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md
@.planning/phases/12-authority-boundary-stabilization/12-01-verify-collapse-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append REPLAY-01..17 block + traceability rows to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (existing structure: phase headers, requirement bullets, traceability table at bottom)
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (lines 81-100 — proposed REPLAY-NN table)
    - .planning/phases/12-authority-boundary-stabilization/12-01-verify-collapse-PLAN.md (precedent for AUTH-NN block addition)
  </read_first>
  <action>
    1. Insert a new section header `### Phase 13 — Replay-with-Edit for Run Bundles` after the Phase 12 (`AUTH-16`) block and before `## Deferred (post-v1)`.
    2. Add 17 unchecked bullets, copying titles verbatim from 13-RESEARCH.md `<phase_requirements>` table (REPLAY-01 through REPLAY-17). Keep wording exact; each bullet is `- [ ] **REPLAY-NN**: <description from research table>`.
    3. Append 17 traceability rows to the `## Traceability` table at end of file: `| REPLAY-NN | Phase 13 | Pending |` for each ID in order.
    4. Update the `**Coverage:**` block (currently `Mapped to phases: 107`) to `Mapped to phases: 124` (107 + 17). Update the v1 totals line `+ 17 Phase 13 (REPLAY-01..17)`.
    5. Do not modify any existing rows. Do not change any AUTH-NN/STRESS-NN/etc statuses.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] \*\*REPLAY-' .planning/REQUIREMENTS.md | grep -qx 17 &amp;&amp; grep -c '^| REPLAY-[0-9][0-9] | Phase 13 | Pending |' .planning/REQUIREMENTS.md | grep -qx 17 &amp;&amp; grep -q 'Mapped to phases: 124' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*REPLAY-' .planning/REQUIREMENTS.md` returns exactly `17`
    - `grep -c '^| REPLAY-[0-9][0-9] | Phase 13 | Pending |' .planning/REQUIREMENTS.md` returns exactly `17`
    - `grep -q 'Phase 13 — Replay-with-Edit for Run Bundles' .planning/REQUIREMENTS.md` succeeds
    - `grep -q 'Mapped to phases: 124' .planning/REQUIREMENTS.md` succeeds
    - No removed lines: `git diff --shortstat .planning/REQUIREMENTS.md` shows 0 deletions
  </acceptance_criteria>
  <done>REPLAY-01..17 are first-class entries in REQUIREMENTS.md with Pending traceability and updated coverage totals.</done>
</task>

<task type="auto">
  <name>Task 2: Write 13-ADR-001 pinning the read-set sidecar surface</name>
  <files>.planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md</files>
  <read_first>
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (Pattern 1: Read-Set Sidecar — option (c) recommended; Pitfall #1 STRESS-07 dependency)
    - .planning/phases/13-replay-with-edit-run-bundles/13-CONTEXT.md (D-07 read-set instrumentation; D-06 hash chain)
    - apps/factory-cli/src/pile-persistence.ts (lines 69-99 — pile artifact writer, the existing fs-tier seam)
  </read_first>
  <action>
    Create the ADR with the following exact section headers and concrete content:
    1. `# ADR-001 — Read-Set Sidecar Surface for Phase 13 (D-07)` heading.
    2. `## Decision` — pin option (c): a uniform `<stage>/pathsRead.json` sidecar at every stage boundary, schema `PathsReadRecord` v1.0.0 as defined in 13-RESEARCH.md "PathsRead Sidecar (REPLAY-05)" code block.
    3. `## On-disk locations` — list verbatim:
       - `runs/&lt;id&gt;/execution/pathsRead.json`
       - `runs/&lt;id&gt;/review/iter-&lt;N&gt;/pathsRead.json`
       - `runs/&lt;id&gt;/piles/&lt;kind&gt;/iter-&lt;N&gt;/pathsRead.json`
       - `runs/&lt;id&gt;/evaluation/&lt;stage&gt;/pathsRead.json`
    4. `## Rationale` — additive (no schema-bump cascade through existing artifacts), uniform shape across stages, pure-tier producers + fs-tier writes co-located at existing seams (pile-persistence.ts, persist-iteration.ts, run-real-execution.ts boundary).
    5. `## Rejected alternatives` — option (a) inline-on-trace and option (b) journal-event extension; cite cascade through `packages/admission-e2e/src/contracts/`.
    6. `## Consequences` — Plans 13-03/04/05 implement against this surface; resolver in plan 13-06 reads PathsReadRecord schemaVersion 1.0.0; per-stage hash-chain harness in plan 13-06 keys by `stageInstance`.
    7. `## STRESS-07 Posture` — record Pitfall #1: D-06 byte-equality on cascaded suffix requires STRESS-07 deterministic mock backend; plan 13-11 (regression fixture) MUST either depend on STRESS-07 landing first OR scope hash-chain assertions to the unchanged prefix only and document cascaded suffix as behavioral verdict-only. Plan 13-11 picks one and pins it.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md &amp;&amp; grep -q '## Decision' .planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md &amp;&amp; grep -q 'pathsRead.json' .planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md &amp;&amp; grep -q 'STRESS-07' .planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 7 section headers above
    - Mentions the four sidecar paths verbatim
    - References "option (c)", "STRESS-07", and the schemaVersion `1.0.0`
    - Cross-references Plan 13-11 posture decision
  </acceptance_criteria>
  <done>Read-set surface is pinned; downstream plans cite this ADR as the contract.</done>
</task>

<task type="auto">
  <name>Task 3: Write 13-PHASE12-WIRING-PRECONDITION.md</name>
  <files>.planning/phases/13-replay-with-edit-run-bundles/13-PHASE12-WIRING-PRECONDITION.md</files>
  <read_first>
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (Pitfall #6: Phase 12 wiring drift)
    - .planning/phases/12-authority-boundary-stabilization/12-06-mechanical-via-repo-and-wiring-PLAN.md (the wiring extraction owner)
    - apps/factory-cli/src/wiring/ (current contents — confirms `command-execution.ts`/`delivery.ts` are NOT yet present; Phase 12 plan 12-06 still pending)
  </read_first>
  <action>
    Create a short precondition note (≤30 lines) with:
    1. `# Phase 12 Plan 12-06 Wiring Extraction Precondition` heading.
    2. Statement: Plans 13-03, 13-04, 13-05, 13-07 touch `apps/factory-cli/src/main.ts` and/or `apps/factory-cli/src/wiring/`. Phase 12 plan `12-06-mechanical-via-repo-and-wiring-PLAN.md` extracts `wiring/{command-execution,delivery}.ts` from `main.ts`.
    3. Rule: before any Phase 13 wave-1+ plan starts, executor MUST verify `apps/factory-cli/src/wiring/command-execution.ts` exists; if absent, halt and surface a checker-style refusal (12-06 has not merged). When 12-06 has merged, Phase 13 sidecar writes register inside the new wiring module that invokes the corresponding stage; replay command registration in plan 13-07 lands in the dispatcher seam established by 12-06.
    4. Verification command for executors: `test -f apps/factory-cli/src/wiring/command-execution.ts || { echo 'PHASE 12 PLAN 12-06 NOT MERGED — refuse'; exit 4; }`.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-replay-with-edit-run-bundles/13-PHASE12-WIRING-PRECONDITION.md &amp;&amp; grep -q 'command-execution.ts' .planning/phases/13-replay-with-edit-run-bundles/13-PHASE12-WIRING-PRECONDITION.md &amp;&amp; grep -q '12-06' .planning/phases/13-replay-with-edit-run-bundles/13-PHASE12-WIRING-PRECONDITION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists; mentions `command-execution.ts`, `12-06`, and the verification command
    - Halt-rule is unambiguous (executor cannot proceed if wiring module absent)
  </acceptance_criteria>
  <done>Phase 13 executors have a documented precondition gate against Phase 12 wiring drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → planning docs | operator-edited markdown influences downstream Claude executors via plan frontmatter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | REQUIREMENTS.md row mismatch with plan frontmatter | mitigate | Plan-checker pass enforces every REPLAY-NN appears in at least one plan's `requirements:` field |
| T-13-01-02 | Repudiation | ADR drift vs. implementation | accept | ADR commit pins decision; Phase 13 retrospective owns drift detection |
</threat_model>

<verification>
- `grep -c 'REPLAY-' .planning/REQUIREMENTS.md` ≥ 34 (17 bullets + 17 traceability rows)
- `git status` shows the three files modified/created above and nothing else
- `pnpm run verify` still green (this plan is markdown-only; should not affect any test)
</verification>

<success_criteria>
- REPLAY-01..17 land in REQUIREMENTS.md with Pending traceability
- 13-ADR-001 pins sidecar surface (option c) and STRESS-07 posture
- 13-PHASE12-WIRING-PRECONDITION pinned before any wiring-touching wave starts
</success_criteria>

<output>
After completion, create `.planning/phases/13-replay-with-edit-run-bundles/13-01-SUMMARY.md`
</output>
