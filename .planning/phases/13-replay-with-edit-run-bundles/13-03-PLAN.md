---
phase: 13-replay-with-edit-run-bundles
plan: 03
type: execute
wave: 1
depends_on: [13-01, 13-02]
files_modified:
  - packages/execution/src/index.ts
  - apps/factory-cli/src/replay-paths-read-writer.ts
  - apps/factory-cli/src/replay-paths-read-writer.test.ts
  - apps/factory-cli/src/run-real-execution.ts
  - apps/factory-cli/src/run-real-execution.test.ts
  - apps/factory-cli/package.json
autonomous: true
requirements: [REPLAY-05]
must_haves:
  truths:
    - "Every execution stage in a recorded run produces `runs/<id>/execution/pathsRead.json` matching PathsReadRecordSchema"
    - "Recorded sidecar enumerates every artifact the execution stage read, with a sha256 input hash and the stage's outputs hash"
    - "Sidecar write is atomic (tmp+rename) and never mutates the bundle on schema-failure"
  artifacts:
    - path: "apps/factory-cli/src/replay-paths-read-writer.ts"
      provides: "writePathsReadSidecar(runDir, stageInstance, record) — atomic tmp+rename"
    - path: "packages/execution/src/index.ts"
      contains: "pathsRead"
    - path: "apps/factory-cli/src/run-real-execution.ts"
      contains: "writePathsReadSidecar"
  key_links:
    - from: "apps/factory-cli/src/run-real-execution.ts"
      to: "apps/factory-cli/src/replay-paths-read-writer.ts"
      via: "post-execution sidecar write call"
      pattern: "writePathsReadSidecar"
    - from: "packages/execution/src/index.ts"
      to: "@protostar/replay PathsReadRecord shape"
      via: "execution returns pathsRead structure consumed by factory-cli writer"
      pattern: "pathsRead"
---

<objective>
Land D-07 read-set instrumentation for the **execution** stage: `packages/execution` reports the bundle-relative artifacts each task read; `apps/factory-cli/src/run-real-execution.ts` writes a `runs/<id>/execution/pathsRead.json` sidecar (atomic tmp+rename, mirroring `pile-persistence.ts:131-145`). Sidecar matches `PathsReadRecordSchema` from `@protostar/replay`.

Purpose: replay's dependency-closure resolver (plan 13-06) needs a recorded read-set per stage. Without execution-stage instrumentation, the cascade computation can't tell whether a pile-output edit affects task results.
Output: Execution-stage sidecar at every real-mode run + the writer helper reused by plans 13-04/13-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-replay-with-edit-run-bundles/13-CONTEXT.md
@.planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md
@.planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md
@.planning/phases/13-replay-with-edit-run-bundles/13-PHASE12-WIRING-PRECONDITION.md
@apps/factory-cli/src/run-real-execution.ts
@apps/factory-cli/src/pile-persistence.ts
@packages/execution/src/index.ts

<interfaces>
<!-- Reuse the existing atomic-write pattern from pile-persistence.ts -->

From apps/factory-cli/src/pile-persistence.ts:131-145 (atomic JSON write pattern):
```typescript
// Pattern: write to <target>.tmp-<random>, then fs.rename to <target>
// Uses sortJsonValue + JSON.stringify for byte-stable output.
```

From @protostar/replay (created in plan 13-02):
```typescript
export const PathsReadRecordSchema: z.ZodType<PathsReadRecord>;
export type PathsReadRecord = {
  schemaVersion: "1.0.0";
  stage: "execution" | "review" | "evaluation" | "planning-pile" | "review-pile" | "exec-coord-pile";
  stageInstance: string;        // e.g. "execution"
  runId: string;
  recordedAt: string;
  pathsRead: ReadonlyArray<{ path: string; jsonPointer?: string; sha256: string }>;
  inputsHash: string;
  outputsHash: string;
};
```

From apps/factory-cli/src/run-real-execution.ts:43,58-73 (existing nowIso seam — reuse for `recordedAt`):
```typescript
export interface RunRealExecutionInput {
  // ...
  readonly nowIso?: () => string;
}
export async function runRealExecution(input: RunRealExecutionInput): Promise<RunRealExecutionResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement replay-paths-read-writer (atomic tmp+rename) + tests</name>
  <files>
    apps/factory-cli/src/replay-paths-read-writer.ts,
    apps/factory-cli/src/replay-paths-read-writer.test.ts,
    apps/factory-cli/package.json
  </files>
  <read_first>
    - apps/factory-cli/src/pile-persistence.ts (lines 131-145 — atomic tmp+rename pattern; copy this approach exactly)
    - packages/replay/src/paths-read-record.ts (PathsReadRecordSchema + type; created by plan 13-02)
    - packages/artifacts/src/canonical-json.ts (sortJsonValue — for byte-stable serialization)
    - apps/factory-cli/src/run-id.ts (RUN_ID_REGEX, assertRunIdConfined — defense-in-depth path validation)
  </read_first>
  <behavior>
    - `writePathsReadSidecar({ runDir, stageInstanceRelDir, record })` validates the record against `PathsReadRecordSchema`, refuses (throws structured error) on schema-fail, computes the target path `<runDir>/<stageInstanceRelDir>/pathsRead.json`, asserts the resolved path stays under `<runDir>` (path-confinement), `mkdir -p` the parent, writes via tmp+rename, and `fs.rename`s atomically.
    - Output bytes are produced by `JSON.stringify(sortJsonValue(record), null, 2) + "\n"` — byte-stable.
    - Refuses if `stageInstanceRelDir` contains `..` or absolute path.
    - Returns `{ path: <absolute path>, sha256: <hash of bytes> }`.
    - Test: writes a valid record; reads back; sha256 of bytes matches return value; second write with same input is byte-identical.
    - Test: refuses schema-invalid record (e.g. non-hex sha256).
    - Test: refuses path traversal (`stageInstanceRelDir = "../escape"` or absolute).
  </behavior>
  <action>
    1. RED: write `replay-paths-read-writer.test.ts` first with the four behaviors above. Run; assert tests fail because the module doesn't exist.
    2. GREEN: implement `writePathsReadSidecar` in `replay-paths-read-writer.ts`. Use `node:fs/promises` (writeFile, rename, mkdir), `node:path` (resolve, relative), `node:crypto` (createHash), `sortJsonValue` from `@protostar/artifacts`, `PathsReadRecordSchema` from `@protostar/replay`.
    3. Add `@protostar/replay` as a dependency to `apps/factory-cli/package.json` (`"workspace:*"`); add a tsconfig reference to `packages/replay`.
    4. `pnpm install && pnpm --filter @protostar/factory-cli build && pnpm --filter @protostar/factory-cli test -- --test-name-pattern '^writePathsReadSidecar'`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; pnpm install &amp;&amp; pnpm --filter @protostar/factory-cli build &amp;&amp; pnpm --filter @protostar/factory-cli test -- --test-name-pattern '^writePathsReadSidecar' &amp;&amp; grep -q 'PathsReadRecordSchema' apps/factory-cli/src/replay-paths-read-writer.ts &amp;&amp; grep -q 'sortJsonValue' apps/factory-cli/src/replay-paths-read-writer.ts &amp;&amp; grep -q 'rename' apps/factory-cli/src/replay-paths-read-writer.ts</automated>
  </verify>
  <acceptance_criteria>
    - `writePathsReadSidecar` function exists and is exported: `grep -q 'export.*writePathsReadSidecar' apps/factory-cli/src/replay-paths-read-writer.ts`
    - Atomic write pattern present: `grep -q 'rename' apps/factory-cli/src/replay-paths-read-writer.ts`
    - Schema validation present: `grep -q 'PathsReadRecordSchema' apps/factory-cli/src/replay-paths-read-writer.ts`
    - Byte-stable serialization: `grep -q 'sortJsonValue' apps/factory-cli/src/replay-paths-read-writer.ts`
    - Tests cover: valid write, schema-reject, path-traversal-reject, byte-stable second write — all four pass
    - `apps/factory-cli/package.json` lists `"@protostar/replay": "workspace:*"`
  </acceptance_criteria>
  <done>Sidecar writer is reusable across plans 13-04 and 13-05 with atomic semantics and schema validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire pathsRead emission into execution stage + factory-cli sidecar write</name>
  <files>
    packages/execution/src/index.ts,
    apps/factory-cli/src/run-real-execution.ts,
    apps/factory-cli/src/run-real-execution.test.ts
  </files>
  <read_first>
    - packages/execution/src/index.ts (current public surface — find the result type returned to factory-cli)
    - apps/factory-cli/src/run-real-execution.ts (lines 31-73 input/result types; lines 102-260 task loop; lines around `evidenceArtifact` for paths read per task)
    - apps/factory-cli/src/run-real-execution.test.ts (existing test pattern to extend)
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (Pattern 1 hook points table — confirms execution sidecar write happens "factory-cli post-call")
    - .planning/phases/13-replay-with-edit-run-bundles/13-PHASE12-WIRING-PRECONDITION.md
  </read_first>
  <behavior>
    - Before starting Task 2, executor runs `test -f apps/factory-cli/src/wiring/command-execution.ts || { echo 'PHASE 12 PLAN 12-06 NOT MERGED — refuse' &gt;&amp;2; exit 4; }` per the precondition note. If 12-06 has NOT merged, halt and surface this state — do not proceed.
    - `packages/execution` returns a `pathsRead` array as part of its result. Each entry: `{ path: string (bundle-relative), jsonPointer?: string, sha256: string }`. For real-mode execution v1, the entries include the `intent.json`, `plan.json`, `execution-plan.json`, and per-task input artifacts the executor consumed. Hand-roll the list in v1 (each task's adapter knows what it read; the loop accumulates).
    - `apps/factory-cli/src/run-real-execution.ts`: after the existing execution loop completes successfully (or fails — sidecar still written), build a `PathsReadRecord` with `stage: "execution"`, `stageInstance: "execution"`, `runId`, `recordedAt: nowIso()`, `pathsRead: <accumulated>`, `inputsHash: sha256(canonical(pathsRead))`, `outputsHash: sha256(canonical(<execution journal snapshot or final result JSON>))`.
    - Call `writePathsReadSidecar({ runDir: <runs/<id>>, stageInstanceRelDir: "execution", record })`.
    - Test: run `runRealExecution` against a fixture; assert `runs/<id>/execution/pathsRead.json` exists, parses as `PathsReadRecordSchema`, lists `intent.json` in pathsRead, has 64-hex `inputsHash` and `outputsHash`.
    - Test: corrupt the sidecar input (force a bad sha256); assert the writer refuses and the run reports the failure (does not silently swallow).
  </behavior>
  <action>
    1. Verify Phase 12 wiring precondition (run the test command above). If wiring/ lacks `command-execution.ts`, halt.
    2. RED: extend `run-real-execution.test.ts` with a new test asserting the sidecar appears at `<runDir>/execution/pathsRead.json` with the expected shape after a successful real-mode execution. Run — should fail (no sidecar yet).
    3. GREEN: in `packages/execution/src/index.ts` (or the appropriate execution-engine entry), thread a `pathsRead` accumulator through the task loop; add it to the return type. Pass `pathsRead` and the canonical execution outputs JSON back to factory-cli.
    4. In `run-real-execution.ts`, after the loop, compute `inputsHash` and `outputsHash` (use `sortJsonValue` + `createHash("sha256")`). Build the `PathsReadRecord`. Call `writePathsReadSidecar`. Place this call in the wiring module if `apps/factory-cli/src/wiring/command-execution.ts` is the relevant invocation site post-12-06; otherwise place it inside `run-real-execution.ts` itself at the result-collection seam.
    5. Run `pnpm --filter @protostar/execution build && pnpm --filter @protostar/factory-cli build && pnpm --filter @protostar/factory-cli test`.
    6. Run `pnpm run verify` to confirm no other suite regresses.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; test -f apps/factory-cli/src/wiring/command-execution.ts &amp;&amp; pnpm --filter @protostar/execution build &amp;&amp; pnpm --filter @protostar/factory-cli build &amp;&amp; pnpm --filter @protostar/factory-cli test &amp;&amp; pnpm run verify &amp;&amp; grep -q 'writePathsReadSidecar' apps/factory-cli/src/run-real-execution.ts &amp;&amp; grep -q 'pathsRead' packages/execution/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - Phase 12 precondition holds: `test -f apps/factory-cli/src/wiring/command-execution.ts` succeeds
    - `grep -q 'writePathsReadSidecar' apps/factory-cli/src/run-real-execution.ts` succeeds
    - `grep -q 'pathsRead' packages/execution/src/index.ts` succeeds (execution emits the field)
    - `pnpm --filter @protostar/factory-cli test` green; the new sidecar test asserts the file exists, schema-validates, and lists at least one path
    - `pnpm run verify` still green at top level
    - Sidecar file produced by a fixture test contains exactly `"stage": "execution"` (literal): test asserts via `grep` on the produced file
  </acceptance_criteria>
  <done>Real-mode execution writes a schema-valid `execution/pathsRead.json` sidecar; downstream cascade resolver has the input it needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execution adapter → bundle fs | sidecar write is the only new fs surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Tampering | sidecar path traversal via crafted stageInstanceRelDir | mitigate | Path-confinement assertion in `writePathsReadSidecar` (Task 1); refuses absolute paths and `..` segments |
| T-13-03-02 | Tampering | partial sidecar write on crash | mitigate | tmp+rename atomic write pattern (mirrors pile-persistence.ts:131-145) |
| T-13-03-03 | Repudiation | sidecar produced for a stage that didn't actually run | mitigate | Writer is invoked from the same call site that completes execution; failed execution still writes the sidecar reflecting partial reads |
</threat_model>

<verification>
- `pnpm run verify` green
- A fixture run produces `runs/<id>/execution/pathsRead.json`
- `PathsReadRecordSchema.parse(JSON.parse(file))` succeeds in test
- Re-run with same inputs → byte-identical sidecar (pre-D-06 hash-chain spot check)
</verification>

<success_criteria>
- REPLAY-05 (execution producer): satisfied
- Sidecar at `runs/<id>/execution/pathsRead.json` exists for every real-mode execution
- Reusable `writePathsReadSidecar` available for plans 13-04 and 13-05
</success_criteria>

<output>
After completion, create `.planning/phases/13-replay-with-edit-run-bundles/13-03-SUMMARY.md`
</output>
