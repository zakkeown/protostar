---
phase: 13-replay-with-edit-run-bundles
plan: 05
type: execute
wave: 1
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - packages/evaluation/src/index.ts
  - packages/evaluation-runner/src/run-evaluation-stages.ts
  - packages/evaluation-runner/src/run-evaluation-stages.test.ts
  - apps/factory-cli/src/run-evaluation.ts
  - apps/factory-cli/src/run-evaluation.test.ts
autonomous: true
requirements: [REPLAY-05]
must_haves:
  truths:
    - "Each evaluation stage (mechanical, semantic, consensus) produces `runs/<id>/evaluation/<stage>/pathsRead.json`"
    - "pathsRead lists the diff, AC, judge inputs, and any prior-stage scores the stage consumed"
    - "Sidecar contents are byte-stable across re-runs with identical inputs"
  artifacts:
    - path: "packages/evaluation-runner/src/run-evaluation-stages.ts"
      contains: "pathsRead"
    - path: "apps/factory-cli/src/run-evaluation.ts"
      contains: "writePathsReadSidecar"
  key_links:
    - from: "packages/evaluation-runner/src/run-evaluation-stages.ts"
      to: "PathsReadRecord shape"
      via: "stage entry/exit boundary returns pathsRead"
      pattern: "pathsRead"
    - from: "apps/factory-cli/src/run-evaluation.ts"
      to: "apps/factory-cli/src/replay-paths-read-writer.ts"
      via: "post-stage sidecar write per evaluation stage"
      pattern: "writePathsReadSidecar"
---

<objective>
Land the read-set sidecar at the **evaluation** stage boundary. `packages/evaluation-runner` returns a per-stage pathsRead record alongside its existing outputs; `apps/factory-cli` writes a sidecar at `runs/<id>/evaluation/<stage>/pathsRead.json` for each stage (mechanical, semantic, consensus) actually executed.

Purpose: counterfactual reports (plan 13-10) and dependency-closure resolution (plan 13-06) require the evaluation stage to record what it read. Phase 8's evolution-runner is also the eventual consumer of `counterfactual.json`, so the evaluation hook point is shared infrastructure.
Output: per-stage evaluation sidecars committed; plan 13-10 builds counterfactual.json on top of these.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-replay-with-edit-run-bundles/13-CONTEXT.md
@.planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md
@.planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md
@packages/evaluation-runner/src/run-evaluation-stages.ts
@apps/factory-cli/src/replay-paths-read-writer.ts

<interfaces>
<!-- Evaluation runner entry point (existing) -->

From packages/evaluation-runner/src/run-evaluation-stages.ts:21,337 (verified):
```typescript
import { decideEvolution } from "@protostar/evaluation";
// line 337: return decideEvolution({ ... existing inputs ... });
```

The existing `runEvaluationStages` (or equivalent function) returns a result tracked per stage. Plan extends each stage's result with an optional `pathsRead?: ReadonlyArray<{ path: string; jsonPointer?: string; sha256: string }>` and the runner collects them into a per-stage map for the caller.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evaluation stages emit pathsRead per stage; runner returns per-stage map</name>
  <files>
    packages/evaluation/src/index.ts,
    packages/evaluation-runner/src/run-evaluation-stages.ts,
    packages/evaluation-runner/src/run-evaluation-stages.test.ts
  </files>
  <read_first>
    - packages/evaluation-runner/src/run-evaluation-stages.ts (full — find each stage's invocation point and the result aggregation)
    - packages/evaluation-runner/src/run-evaluation-stages.test.ts (current test patterns)
    - packages/evaluation/src/index.ts (mechanical / semantic / consensus stage entry types)
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (Pattern 1 — evaluation sidecar location `runs/<id>/evaluation/<stage>/pathsRead.json`; hook table — "Stage entry/exit boundary; runner returns record")
  </read_first>
  <behavior>
    - Each evaluation stage function (mechanical, semantic, consensus) returns its existing result PLUS an explicit `pathsRead: ReadonlyArray<{ path: string; jsonPointer?: string; sha256: string }>` declared in its return type. Hand-roll the list per stage based on what each stage actually consumes (mechanical reads diff/AC/lint output paths; semantic reads judge input prompt + diff; consensus reads the semantic verdict + a second judge prompt).
    - `runEvaluationStages` (or equivalent) returns a per-stage map: `{ mechanical?: PathsReadStageInfo, semantic?: PathsReadStageInfo, consensus?: PathsReadStageInfo }` where each entry carries `{ pathsRead, outputsCanonicalJson }`.
    - Skipped stages (e.g. consensus when not invoked) do not appear in the map.
    - Test: drive a fake stage runner; assert mechanical stage returns `pathsRead` length ≥ 1, every entry has 64-hex sha256.
    - Test: per-stage map is non-empty for stages that ran; absent for stages that skipped.
  </behavior>
  <action>
    1. RED: extend `run-evaluation-stages.test.ts` with the two behaviors above. Run; verify failure.
    2. GREEN: thread a `pathsRead` accumulator through each stage function in `packages/evaluation/src/index.ts`. Update return types in the index barrel. The runner aggregates them into the per-stage map and returns it as part of its result.
    3. Make sure the runner remains pure with respect to fs — it returns the data, factory-cli writes the sidecar in Task 2.
    4. `pnpm --filter @protostar/evaluation build/test && pnpm --filter @protostar/evaluation-runner build/test`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; pnpm --filter @protostar/evaluation build &amp;&amp; pnpm --filter @protostar/evaluation test &amp;&amp; pnpm --filter @protostar/evaluation-runner build &amp;&amp; pnpm --filter @protostar/evaluation-runner test &amp;&amp; grep -q 'pathsRead' packages/evaluation-runner/src/run-evaluation-stages.ts &amp;&amp; grep -q 'pathsRead' packages/evaluation/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'pathsRead' packages/evaluation/src/index.ts` succeeds
    - `grep -q 'pathsRead' packages/evaluation-runner/src/run-evaluation-stages.ts` succeeds
    - Test: stage map contains `mechanical` (length ≥ 1) for a fixture run; `consensus` is absent when not executed
    - `pnpm --filter @protostar/evaluation-runner test` green; new behaviors covered
    - `packages/evaluation-runner` no-fs contract test still passes (this stays network-tier; pure stage functions live in `@protostar/evaluation`)
  </acceptance_criteria>
  <done>Evaluation runner returns structured per-stage read-set data; factory-cli can persist it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: factory-cli writes per-stage evaluation pathsRead.json sidecars</name>
  <files>
    apps/factory-cli/src/run-evaluation.ts,
    apps/factory-cli/src/run-evaluation.test.ts
  </files>
  <read_first>
    - apps/factory-cli/src/run-evaluation.ts (existing — current evaluation invocation seam in factory-cli)
    - apps/factory-cli/src/replay-paths-read-writer.ts (writer from plan 13-03)
    - packages/evaluation-runner/src/run-evaluation-stages.ts (the per-stage map produced by Task 1)
  </read_first>
  <behavior>
    - After `runEvaluationStages` returns, factory-cli iterates the per-stage map. For each stage that ran, it builds a `PathsReadRecord` with `stage: "evaluation"`, `stageInstance: \`evaluation/${stageName}\``, `runId`, `recordedAt`, `pathsRead`, `inputsHash` (over canonical sorted pathsRead), `outputsHash` (over canonical stage output JSON).
    - Calls `writePathsReadSidecar({ runDir, stageInstanceRelDir: \`evaluation/${stageName}\`, record })` for each stage in the map.
    - Test (RED→GREEN): drive a fixture evaluation; assert files at `evaluation/mechanical/pathsRead.json` exist; schema-validate; sidecar `stageInstance` matches the directory name.
    - Test: skipped consensus stage produces no `evaluation/consensus/pathsRead.json` file.
  </behavior>
  <action>
    1. RED: extend `run-evaluation.test.ts` with the two behaviors. Run; verify failure.
    2. GREEN: in `run-evaluation.ts`, after the runner call, iterate the per-stage map and call `writePathsReadSidecar` for each present entry. Use `nowIso()` (already threaded through) for `recordedAt`.
    3. Run `pnpm --filter @protostar/factory-cli build/test && pnpm run verify`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; pnpm --filter @protostar/factory-cli build &amp;&amp; pnpm --filter @protostar/factory-cli test &amp;&amp; pnpm run verify &amp;&amp; grep -q 'writePathsReadSidecar' apps/factory-cli/src/run-evaluation.ts &amp;&amp; grep -q 'evaluation/' apps/factory-cli/src/run-evaluation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'writePathsReadSidecar' apps/factory-cli/src/run-evaluation.ts` succeeds
    - Fixture-driven test asserts `evaluation/mechanical/pathsRead.json` exists with valid schema
    - Skipped-stage test asserts the corresponding sidecar is absent
    - `pnpm run verify` green
  </acceptance_criteria>
  <done>Per-stage evaluation sidecars are recorded; replay's cascade resolver and counterfactual writer have the inputs they need.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluation-runner (network tier) → bundle fs (factory-cli tier) | runner returns pure data; factory-cli holds the only fs authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Tampering | evaluation-runner accidentally gains fs authority via pathsRead write | mitigate | Runner returns plain data; factory-cli writes; no-fs contract on evaluation-runner remains green |
| T-13-05-02 | Information Disclosure | sidecar leaks internal prompt-cache state | accept | Sidecar lists artifact paths and hashes; prompt content is not recorded here (lives elsewhere) |
| T-13-05-03 | Tampering | sidecar mismatch with stage outcome on partial failure | mitigate | Sidecar is written only when stage successfully returns; failures skip both stage output and sidecar atomically |
</threat_model>

<verification>
- `pnpm run verify` green
- Fixture run produces `evaluation/mechanical/pathsRead.json`, `evaluation/semantic/pathsRead.json` (when semantic runs)
- All sidecars schema-validate
- evaluation-runner no-fs contract unaffected
</verification>

<success_criteria>
- REPLAY-05 (evaluation producer): satisfied
- Per-stage sidecars committed alongside existing evaluation outputs
- Plan 13-10 (counterfactual report) can compute eval delta from these sidecars
</success_criteria>

<output>
After completion, create `.planning/phases/13-replay-with-edit-run-bundles/13-05-SUMMARY.md`
</output>
