---
phase: 13-replay-with-edit-run-bundles
plan: 11
type: execute
wave: 4
depends_on: [13-06, 13-07, 13-08, 13-09, 13-10]
files_modified:
  - packages/admission-e2e/src/replay/parent-bundle/manifest.json
  - packages/admission-e2e/src/replay/parent-bundle/intent.json
  - packages/admission-e2e/src/replay/parent-bundle/plan.json
  - packages/admission-e2e/src/replay/parent-bundle/execution-plan.json
  - packages/admission-e2e/src/replay/parent-bundle/piles/review/iter-1/result.json
  - packages/admission-e2e/src/replay/parent-bundle/piles/review/iter-1/trace.json
  - packages/admission-e2e/src/replay/parent-bundle/piles/review/iter-1/pathsRead.json
  - packages/admission-e2e/src/replay/parent-bundle/execution/pathsRead.json
  - packages/admission-e2e/src/replay/parent-bundle/review/iter-1/pathsRead.json
  - packages/admission-e2e/src/replay/parent-bundle/evaluation/mechanical/pathsRead.json
  - packages/admission-e2e/src/replay/parent-bundle/evaluation-report.json
  - packages/admission-e2e/src/replay/edit-set-pass.json
  - packages/admission-e2e/src/replay/edit-set-still-fail.json
  - packages/admission-e2e/src/replay/expected-cascade.json
  - packages/admission-e2e/src/replay/expected-hash-chain.json
  - packages/admission-e2e/src/replay/replay-pass.contract.test.ts
  - packages/admission-e2e/src/replay/replay-still-fail.contract.test.ts
  - packages/admission-e2e/src/replay/STRESS-07-POSTURE.md
autonomous: true
requirements: [REPLAY-16]
must_haves:
  truths:
    - "A committed parent run bundle exists at `packages/admission-e2e/src/replay/parent-bundle/` with a recorded failing-run shape"
    - "edit-set-pass.json edits a single judge verdict in `piles/review/iter-1/result.json` such that replay verdict flips to pass"
    - "edit-set-still-fail.json edits the same artifact differently such that replay verdict stays fail"
    - "Both fixtures replay deterministically: per-stage hash-chain assertion (D-06) on the unchanged prefix is byte-equal across re-runs; cascaded suffix posture matches the STRESS-07 decision pinned in 13-ADR-001"
    - "`replays/<replayId>/` sibling layout is verified; original bundle bytes are unchanged"
    - "`evaluation/counterfactual.json` is produced and validates against schema; pass-fixture has verdictChanged=true"
  artifacts:
    - path: "packages/admission-e2e/src/replay/parent-bundle/manifest.json"
      provides: "committed failing-run fixture"
    - path: "packages/admission-e2e/src/replay/edit-set-pass.json"
      provides: "EditSet that flips verdict to pass"
    - path: "packages/admission-e2e/src/replay/edit-set-still-fail.json"
      provides: "EditSet that does not flip verdict"
    - path: "packages/admission-e2e/src/replay/replay-pass.contract.test.ts"
      provides: "edit→replay→pass regression contract"
    - path: "packages/admission-e2e/src/replay/replay-still-fail.contract.test.ts"
      provides: "edit→replay→still-fail regression contract"
  key_links:
    - from: "packages/admission-e2e/src/replay/replay-pass.contract.test.ts"
      to: "factory-cli replay command"
      via: "execAsync(`protostar-factory replay --run <fixture-id> --edit-set edit-set-pass.json`)"
      pattern: "protostar-factory replay|executeReplay"
    - from: "packages/admission-e2e/src/replay/replay-pass.contract.test.ts"
      to: "@protostar/replay assertHashChain"
      via: "D-06 per-stage hash-chain assertion"
      pattern: "assertHashChain"
---

<objective>
The committed regression fixture demanded by ROADMAP success criteria 4 ("failing-run regression fixture in `packages/admission-e2e/src/replay/` showing edit→replay→pass and edit→replay→still-fail paths"). Two contract tests drive the full replay pipeline end-to-end against a committed parent bundle and pin determinism via `assertHashChain`.

The plan also pins the **STRESS-07 posture** (per 13-ADR-001 Task 2): does the hash-chain assertion enforce byte-equality on the cascaded suffix, or only on the unchanged prefix? This plan picks one decision based on STRESS-07's status at the time of execution and commits the choice in `STRESS-07-POSTURE.md`.

Purpose: the fixture is the regression substrate. Without it, no future replay-related change can be detected as drift; D-06 hash-chain is unobservable.
Output: complete fixture + 2 contract tests + posture decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-replay-with-edit-run-bundles/13-CONTEXT.md
@.planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md
@.planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md
@packages/replay/src/index.ts
@apps/factory-cli/src/commands/replay.ts
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Fixture inputs the contract tests drive against -->

From @protostar/replay (plans 13-02, 13-06, 13-10):
```typescript
export const EditSetSchema, PathsReadRecordSchema, OverlayManifestSchema, CounterfactualReportSchema;
export function parseEditAddress, isPileOutputPath, computeEditSetHash;
export function resolveCascade, assertHashChain, computeCounterfactualDelta;
```

From plan 13-07: `protostar-factory replay --run <id> --edit-set <path>` produces `runs/<parentRunId>/replays/<replayId>/`.

From plan 13-09: `--allow-delivery` is required for delivery; this plan does NOT exercise delivery (it stays in dark-replay mode per D-12 default).

STRESS-07 status (from REQUIREMENTS.md line 323): **Pending** as of 2026-04-29.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin STRESS-07 posture + commit fixture parent bundle</name>
  <files>
    packages/admission-e2e/src/replay/STRESS-07-POSTURE.md,
    packages/admission-e2e/src/replay/parent-bundle/manifest.json,
    packages/admission-e2e/src/replay/parent-bundle/intent.json,
    packages/admission-e2e/src/replay/parent-bundle/plan.json,
    packages/admission-e2e/src/replay/parent-bundle/execution-plan.json,
    packages/admission-e2e/src/replay/parent-bundle/piles/review/iter-1/result.json,
    packages/admission-e2e/src/replay/parent-bundle/piles/review/iter-1/trace.json,
    packages/admission-e2e/src/replay/parent-bundle/piles/review/iter-1/pathsRead.json,
    packages/admission-e2e/src/replay/parent-bundle/execution/pathsRead.json,
    packages/admission-e2e/src/replay/parent-bundle/review/iter-1/pathsRead.json,
    packages/admission-e2e/src/replay/parent-bundle/evaluation/mechanical/pathsRead.json,
    packages/admission-e2e/src/replay/parent-bundle/evaluation-report.json
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 323 — STRESS-07 status)
    - .planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md (STRESS-07 Posture section)
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (Pitfall #1 — STRESS-07 dependency; Pattern 1 — sidecar layout)
    - apps/factory-cli/src/pile-persistence.ts (pile artifact write site — for fixture shape correctness)
    - packages/replay/src/paths-read-record.ts (PathsReadRecord schema)
  </read_first>
  <action>
    1. Read `.planning/REQUIREMENTS.md` line for `STRESS-07`. If status is "Complete" → posture = "full-chain" (cascaded suffix is byte-deterministic). If status is "Pending" or anything else → posture = "prefix-only" (unchanged prefix byte-equal; cascaded suffix asserts verdict pass/fail and schema-valid output, NOT byte-equality).
    2. Write `STRESS-07-POSTURE.md` (≤30 lines) recording the chosen posture, the STRESS-07 status as observed, and the exact rule the contract tests enforce. Include: "If STRESS-07 lands later, this fixture's expected-hash-chain.json must be regenerated to lock cascaded-suffix bytes."
    3. Hand-craft a committed parent-bundle directory representing a failing run (mechanical=fail or model=fail) on a tiny synthetic intent/plan. Files to create with byte-stable canonical JSON:
       - `manifest.json` — minimal `FactoryRunManifest` with `status: "blocked"`, `runId: "fixture-replay-parent"`, all required fields. No replay extensions.
       - `intent.json`, `plan.json`, `execution-plan.json` — minimal valid shapes (use existing fixture shapes from `packages/fixtures/__fixtures__/` as a reference; ≤20 lines each).
       - `piles/review/iter-1/result.json` — review pile output with a single judge finding `verdict: "fail"`. This is the artifact the EditSets target.
       - `piles/review/iter-1/trace.json` — minimal trace (timestamp, prompt hash).
       - `piles/review/iter-1/pathsRead.json` — valid PathsReadRecord (`stage: "review-pile"`, lists intent.json, plan.json).
       - `execution/pathsRead.json` — valid PathsReadRecord (`stage: "execution"`).
       - `review/iter-1/pathsRead.json` — valid PathsReadRecord (`stage: "review"`, lists `piles/review/iter-1/result.json` to express the dependency edge).
       - `evaluation/mechanical/pathsRead.json` — valid PathsReadRecord (`stage: "evaluation"`).
       - `evaluation-report.json` — `{ verdict: "fail", score: 0.4, ... }`.
    4. Every JSON file must be byte-stable (sorted keys via `sortJsonValue` semantics). Each `pathsRead.json` must validate against `PathsReadRecordSchema`; `manifest.json` against `FactoryRunManifestSchema`; etc.
    5. Commit only the fixture; no test code yet (Task 2 adds it).
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; test -f packages/admission-e2e/src/replay/parent-bundle/manifest.json &amp;&amp; test -f packages/admission-e2e/src/replay/parent-bundle/piles/review/iter-1/result.json &amp;&amp; test -f packages/admission-e2e/src/replay/parent-bundle/evaluation-report.json &amp;&amp; test -f packages/admission-e2e/src/replay/STRESS-07-POSTURE.md &amp;&amp; node -e 'const {PathsReadRecordSchema} = require("./packages/replay/dist/src/index.js"); const r = require("./packages/admission-e2e/src/replay/parent-bundle/execution/pathsRead.json"); PathsReadRecordSchema.parse(r);'</automated>
  </verify>
  <acceptance_criteria>
    - All 12 fixture files exist
    - `STRESS-07-POSTURE.md` records the active posture (`full-chain` or `prefix-only`) AND the STRESS-07 status observed
    - Every `pathsRead.json` schema-validates against `PathsReadRecordSchema`
    - `evaluation-report.json` has `verdict: "fail"` (parent is a failing run)
    - `piles/review/iter-1/result.json` has at least one finding with `verdict: "fail"` (the edit target)
    - No external symlinks, no absolute paths
  </acceptance_criteria>
  <done>Fixture parent bundle is committed and posture is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: edit-set-pass + edit-set-still-fail + expected-cascade + expected-hash-chain</name>
  <files>
    packages/admission-e2e/src/replay/edit-set-pass.json,
    packages/admission-e2e/src/replay/edit-set-still-fail.json,
    packages/admission-e2e/src/replay/expected-cascade.json,
    packages/admission-e2e/src/replay/expected-hash-chain.json
  </files>
  <read_first>
    - packages/admission-e2e/src/replay/parent-bundle/piles/review/iter-1/result.json (the edit target)
    - packages/replay/src/edit-set/schema.ts (EditSetSchema for shape)
    - packages/replay/src/dependency-closure/resolve.ts (resolveCascade — to manually compute the expected cascade for the fixture topology)
    - packages/admission-e2e/src/replay/parent-bundle/evaluation-report.json
  </read_first>
  <action>
    1. Author `edit-set-pass.json` per `EditSetSchema`:
       ```json
       {
         "schemaVersion": "1.0.0",
         "parentRunId": "fixture-replay-parent",
         "edits": [{
           "path": "piles/review/iter-1/result.json",
           "jsonPointer": "/findings/0/verdict",
           "replacementFile": "./edit-set-pass-replacement.json"
         }]
       }
       ```
       Plus a sibling `edit-set-pass-replacement.json` containing `"pass"`. (Or inline a whole-artifact replacement — pick whichever the EditSet schema permits with a single edit; pin one consistent shape.)
    2. Author `edit-set-still-fail.json` similarly, but the replacement keeps the verdict at `"fail"` (perhaps tweaks confidence or message — semantically still a fail).
    3. Author `expected-cascade.json` — the deterministic output of `resolveCascade` for editing `piles/review/iter-1/result.json` against the fixture's PathsReadRecords. Compute this by hand using the algorithm in plan 13-06: review-pile-iter-1 → review/iter-1 → evaluation/mechanical (depending on which stages list the edited path or its outputs).
    4. Author `expected-hash-chain.json` — the recorded `(stageInstance, inputsHash, outputsHash)` triples for every stage in the fixture, in topo order. For the **unchanged prefix** (e.g. `execution`, `piles/planning/iter-1` if present), pin the exact hashes computed from the fixture artifacts (use a one-shot helper script to compute and commit). For the **cascaded suffix**, the entries depend on STRESS-07 posture from Task 1: if `prefix-only`, omit cascaded entries (or mark them with `"byteCheck": false`); if `full-chain`, include them with locked hashes.
    5. Validate every fixture against the appropriate schema before committing.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; node -e 'const {EditSetSchema} = require("./packages/replay/dist/src/index.js"); EditSetSchema.parse(require("./packages/admission-e2e/src/replay/edit-set-pass.json")); EditSetSchema.parse(require("./packages/admission-e2e/src/replay/edit-set-still-fail.json"));' &amp;&amp; test -f packages/admission-e2e/src/replay/expected-cascade.json &amp;&amp; test -f packages/admission-e2e/src/replay/expected-hash-chain.json</automated>
  </verify>
  <acceptance_criteria>
    - Both EditSets validate against `EditSetSchema`
    - Both EditSets target the same path (`piles/review/iter-1/result.json`) — single-edit ceiling honored
    - `expected-cascade.json` lists at least review/iter-1 and evaluation/mechanical (downstream of the edit)
    - `expected-hash-chain.json` shape matches the input expected by `assertHashChain`: list of `{ stageInstance, inputsHash, outputsHash }` triples
    - Posture-consistent: prefix-only mode omits cascaded-stage hashes (or marks them) per `STRESS-07-POSTURE.md`
  </acceptance_criteria>
  <done>EditSets and expected-output fixtures are committed and schema-valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: replay-pass + replay-still-fail contract tests</name>
  <files>
    packages/admission-e2e/src/replay/replay-pass.contract.test.ts,
    packages/admission-e2e/src/replay/replay-still-fail.contract.test.ts
  </files>
  <read_first>
    - packages/admission-e2e/src/contracts/ (existing contract test patterns — driver shape using node:test, exec, JSON snapshot assertions)
    - apps/factory-cli/src/commands/replay.ts (executeReplay export — preferred to invoking the bin)
    - packages/replay/src/hash-chain/assertions.ts (assertHashChain signature)
    - packages/admission-e2e/src/replay/STRESS-07-POSTURE.md (posture decision drives hash-chain assertion mode)
  </read_first>
  <behavior>
    - `replay-pass.contract.test.ts` flow:
      1. Set up a tmp `runs/<parentRunId>/` directory by copying the committed parent bundle there.
      2. Invoke replay: either spawn `protostar-factory replay --run fixture-replay-parent --edit-set <path>` OR call `executeReplay({...})` directly (preferred — avoids subprocess flakiness in CI).
      3. Assert exit code Ok.
      4. Assert `runs/<parentRunId>/replays/<replayId>/manifest.json` exists; manifest carries `parentRunId`, `replayId`, `editSetHash`, `allowDelivery: false`, `pathsCascaded` matches `expected-cascade.json`.
      5. Assert `replays/<replayId>/overlay.json` exists and lists every preserved artifact with its parent sha256.
      6. Assert ORIGINAL bundle bytes are unchanged: compute sha256 of every committed parent file and verify identity.
      7. Build `actualHashChain` from `replays/<replayId>/.../pathsRead.json` of cascaded stages PLUS the parent-bundle prefix. Call `assertHashChain({ actual: actualHashChain, expected: expectedHashChain, cascadedStages: pathsCascaded })`. Assert `{ ok: true }`.
      8. Assert `replays/<replayId>/evaluation/counterfactual.json` exists with `delta.verdictChanged === true` and `replayEvaluation.verdict === "pass"`.
    - `replay-still-fail.contract.test.ts` mirrors steps 1–7 with `edit-set-still-fail.json`. For step 8: assert `delta.verdictChanged === false` and `replayEvaluation.verdict === "fail"`.
    - Both tests run twice (idempotency check): second invocation produces a byte-identical replay bundle when run with same inputs (or if randomness is involved in replayId, hash the file *contents* excluding the replayId field).
    - When STRESS-07 posture is `prefix-only`, the contract calls `assertHashChain` with `cascadedStages` equal to the actually-cascaded stages so byte-equality is enforced only on the prefix. When posture is `full-chain`, `cascadedStages: []` is passed to enforce the full chain.
  </behavior>
  <action>
    1. RED: write both contract tests with the full flow above. Verify failure (replay command may not exist yet, hash-chain harness usage not yet wired into a contract — both pass after the fixture is exercised end-to-end).
    2. GREEN: ensure all prior plans are merged. Run the contract tests; iterate until both pass under the chosen STRESS-07 posture.
    3. Add the new contract files to `packages/admission-e2e`'s test discovery (if discovery is glob-based, no change; otherwise extend the runner config).
    4. `pnpm --filter @protostar/admission-e2e build/test && pnpm run verify`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; pnpm --filter @protostar/admission-e2e build &amp;&amp; pnpm --filter @protostar/admission-e2e test &amp;&amp; pnpm run verify &amp;&amp; grep -q 'assertHashChain' packages/admission-e2e/src/replay/replay-pass.contract.test.ts &amp;&amp; grep -q 'verdictChanged' packages/admission-e2e/src/replay/replay-pass.contract.test.ts &amp;&amp; grep -q 'verdictChanged' packages/admission-e2e/src/replay/replay-still-fail.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both contract test files exist and pass
    - `grep -q 'assertHashChain' packages/admission-e2e/src/replay/replay-pass.contract.test.ts` succeeds
    - `replay-pass` test asserts `verdictChanged === true`; `replay-still-fail` asserts `verdictChanged === false`
    - Original bundle byte-equality check is present in BOTH tests (parent-bundle untouched)
    - `pathsCascaded` from manifest matches `expected-cascade.json`
    - Hash-chain assertion mode matches `STRESS-07-POSTURE.md`
    - `pnpm run verify` green
    - Contract tests are idempotent: running them twice in succession both pass
  </acceptance_criteria>
  <done>Phase 13 has a committed regression fixture; D-06 hash chain is enforced under the active STRESS-07 posture; both edit→replay→pass and edit→replay→still-fail paths are pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture → CI | fixture drift breaks the regression test; locked by hash-chain assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11-01 | Tampering | accidental fixture mutation in a future PR | mitigate | hash-chain assertions catch any byte-change in the unchanged prefix; expected-hash-chain.json is committed |
| T-13-11-02 | Repudiation | LLM nondeterminism produces flake on cascaded suffix | mitigate | STRESS-07-POSTURE.md decides full-chain vs prefix-only; prefix-only mode treats cascaded suffix as behavioral-only (verdict + schema), not byte-equal |
| T-13-11-03 | Tampering | replay command silently mutates parent bundle | mitigate | Test asserts every committed parent file's sha256 is unchanged after replay |
| T-13-11-04 | EoP | regression fixture accidentally exercises real delivery | mitigate | Tests do NOT pass `--allow-delivery`; replay-config has no `allowDelivery: true`; manifest asserted to have allowDelivery=false |
</threat_model>

<verification>
- `pnpm run verify` green
- Both contract tests pass twice in succession (idempotency)
- Parent bundle byte-equality verified by both tests
- Counterfactual verdict-flip and verdict-stable behaviors both pinned
</verification>

<success_criteria>
- REPLAY-16 (regression fixture for both pass and still-fail): satisfied
- ROADMAP success criterion 4 (failing-run regression fixture): satisfied
- D-06 hash-chain enforcement is pinned at the chosen STRESS-07 posture
- Phase 13 ships with a substrate that detects any future replay-related drift
</success_criteria>

<output>
After completion, create `.planning/phases/13-replay-with-edit-run-bundles/13-11-SUMMARY.md`
</output>
