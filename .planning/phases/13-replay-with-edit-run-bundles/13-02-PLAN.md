---
phase: 13-replay-with-edit-run-bundles
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/replay/package.json
  - packages/replay/tsconfig.json
  - packages/replay/src/index.ts
  - packages/replay/src/edit-set/schema.ts
  - packages/replay/src/edit-set/schema.test.ts
  - packages/replay/src/edit-set/parse-address.ts
  - packages/replay/src/edit-set/parse-address.test.ts
  - packages/replay/src/edit-set/allowlist.ts
  - packages/replay/src/edit-set/allowlist.test.ts
  - packages/replay/src/edit-set/hash.ts
  - packages/replay/src/edit-set/hash.test.ts
  - packages/replay/src/json-pointer.ts
  - packages/replay/src/json-pointer.test.ts
  - packages/replay/src/paths-read-record.ts
  - packages/replay/src/paths-read-record.test.ts
  - packages/replay/src/overlay-manifest/schema.ts
  - packages/replay/src/overlay-manifest/schema.test.ts
  - packages/replay/src/counterfactual/schema.ts
  - packages/replay/src/counterfactual/schema.test.ts
  - packages/replay/src/schema-registry/index.ts
  - packages/replay/src/schema-registry/index.test.ts
  - packages/replay/src/no-fs.contract.test.ts
  - packages/replay/src/no-net.contract.test.ts
  - packages/replay/AGENTS.md
  - packages/replay/README.md
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - packages/admission-e2e/src/contracts/tier-conformance.contract.test.ts
autonomous: true
requirements: [REPLAY-01, REPLAY-02, REPLAY-03, REPLAY-04]
must_haves:
  truths:
    - "@protostar/replay workspace exists at tier=pure with no fs/net imports"
    - "EditSet, OverlayManifest, PathsReadRecord, CounterfactualReport zod schemas validate canonical examples and reject malformed inputs"
    - "Edit-address parser accepts `path` and `path#/json/pointer` forms and refuses everything else"
    - "Pile-output allowlist matches piles/{planning,review,execution-coordination}/iter-N/{result,trace}.json and refuses every other path"
    - "Hand-rolled RFC 6901 JSON Pointer resolves and rejects malformed pointers"
  artifacts:
    - path: "packages/replay/package.json"
      contains: '"protostar.tier": "pure"'
    - path: "packages/replay/src/edit-set/schema.ts"
      provides: "EditSetSchema, EditSchema (zod), EditSet type"
    - path: "packages/replay/src/json-pointer.ts"
      provides: "parsePointer, resolvePointer, setPointer (RFC 6901)"
    - path: "packages/replay/src/no-fs.contract.test.ts"
      provides: "asserts no node:fs / node:fs/promises imports in src/"
    - path: "packages/replay/src/paths-read-record.ts"
      provides: "PathsReadRecordSchema (zod) v1.0.0"
  key_links:
    - from: "packages/replay/src/index.ts"
      to: "all submodule exports"
      via: "barrel re-export"
      pattern: "export \\* from"
    - from: "packages/admission-e2e/src/contracts/tier-conformance.contract.test.ts"
      to: "packages/replay package.json"
      via: "tier=pure assertion + no-fs/no-net contract presence"
      pattern: "@protostar/replay"
---

<objective>
Stand up `@protostar/replay` as a tier=pure workspace owning every Phase 13 schema and pure-tier helper. This is the contract-first plan: every downstream Phase 13 plan imports types and schemas from here. No filesystem I/O. No network I/O. Zero runtime dependencies beyond zod (already a repo dep) and `node:crypto` (built-in).

Purpose: define the shapes consumed by waves 1+ (read-set instrumentation, dependency-closure resolver, overlay writer, counterfactual report) before any producer or fs-tier code exists.
Output: a fully-tested pure-tier package + tier-conformance entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-replay-with-edit-run-bundles/13-CONTEXT.md
@.planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md
@.planning/phases/13-replay-with-edit-run-bundles/13-ADR-001-readset-sidecar.md
@packages/intent/package.json
@packages/intent/tsconfig.json
@packages/artifacts/src/canonical-json.ts
@apps/factory-cli/src/pile-persistence.ts

<interfaces>
<!-- Existing contract-tested helpers the executor MUST reuse, not re-implement -->

From packages/artifacts/src/canonical-json.ts (already in repo, contract-tested):
```typescript
export function sortJsonValue(value: unknown): unknown;  // canonical JSON sort for hashing
```

From packages/authority/src/admission-decision/base.ts:3-10 (the closed GateName enum):
```typescript
export type GateName =
  | "intent"
  | "planning"
  | "capability"
  | "repo-scope"
  | "workspace-trust"
  | "repo-runtime"
  | "coder-adapter-ready";
```
NOTE: this is a closed enum. Plan 13-09 must extend it to add `"replay-delivery"`.

Pile path shape (verified at apps/factory-cli/src/pile-persistence.ts:7-12):
- `piles/<kind>/iter-<N>/result.json`
- `piles/<kind>/iter-<N>/trace.json`
- `<kind>` ∈ `{planning, review, execution-coordination}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold @protostar/replay workspace + schemas + JSON Pointer + allowlist</name>
  <files>
    packages/replay/package.json,
    packages/replay/tsconfig.json,
    packages/replay/AGENTS.md,
    packages/replay/README.md,
    packages/replay/src/index.ts,
    packages/replay/src/edit-set/schema.ts,
    packages/replay/src/edit-set/schema.test.ts,
    packages/replay/src/edit-set/parse-address.ts,
    packages/replay/src/edit-set/parse-address.test.ts,
    packages/replay/src/edit-set/allowlist.ts,
    packages/replay/src/edit-set/allowlist.test.ts,
    packages/replay/src/edit-set/hash.ts,
    packages/replay/src/edit-set/hash.test.ts,
    packages/replay/src/json-pointer.ts,
    packages/replay/src/json-pointer.test.ts,
    pnpm-workspace.yaml,
    tsconfig.base.json
  </files>
  <read_first>
    - packages/intent/package.json (precedent: pure-tier manifest with `protostar.tier: "pure"`, `sideEffects: false`, `engines.node: ">=22"`, scripts.build/test, zod dep version)
    - packages/intent/tsconfig.json (precedent: extends `../../tsconfig.base.json`, references list)
    - packages/intent/AGENTS.md (precedent: short authority/tier statement)
    - pnpm-workspace.yaml (current workspace list — append `packages/replay`)
    - tsconfig.base.json (paths map — add `@protostar/replay` and `@protostar/replay/*` aliases)
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (Code Examples section — copy schema literals verbatim)
  </read_first>
  <behavior>
    - EditSchema: parses `{ path: "piles/review/iter-2/result.json", jsonPointer: "/findings/3", replacementFile: "./fix.json" }`; rejects path outside pile allowlist regex; rejects malformed JSON Pointer.
    - EditSetSchema: parses `{ schemaVersion: "1.0.0", parentRunId: "run-x", edits: [<one EditSchema>] }`; rejects empty edits array; rejects schemaVersion ≠ `"1.0.0"`.
    - parseEditAddress(`piles/review/iter-2/result.json`) → `{ path, jsonPointer: undefined }`. parseEditAddress(`piles/review/iter-2/result.json#/findings/3`) → `{ path, jsonPointer: "/findings/3" }`. Refuses two `#` chars.
    - isPileOutputPath: returns true for `piles/<kind>/iter-<N>/result.json` and `.../trace.json` for kind ∈ planning|review|execution-coordination; false for `manifest.json`, `intent.json`, `delivery/authorization.json`, `../escape`, absolute paths.
    - JSON Pointer parse: `""` → []; `"/foo/bar"` → `["foo", "bar"]`; `"/a~1b"` → `["a/b"]`; `"/a~0b"` → `["a~b"]`; refuses pointers not starting with `/` (when non-empty) and pointers with bare `~` not followed by 0/1.
    - JSON Pointer resolve: walks the pointer; returns `undefined` on missing key; throws on type mismatch (array index ≠ integer).
    - editSetHash: sha256 (hex, 64 chars) over canonical-JSON-serialized EditSet using `sortJsonValue` from `@protostar/artifacts` + `node:crypto.createHash("sha256")`. Idempotent: same EditSet → same hex digest.
  </behavior>
  <action>
    1. Create `packages/replay/package.json` mirroring `packages/intent/package.json` shape exactly:
       - `"name": "@protostar/replay"`, `"version": "0.0.0"`, `"private": true` (until publication parity decided),
       - `"type": "module"`, `"main": "dist/src/index.js"`, `"types": "dist/src/index.d.ts"`,
       - `"exports": { ".": { "types": "./dist/src/index.d.ts", "default": "./dist/src/index.js" } }`,
       - `"sideEffects": false`,
       - `"engines": { "node": ">=22" }`,
       - `"scripts": { "build": "tsc -b", "test": "node --test \"dist/**/*.test.js\"", "clean": "rm -rf dist" }`,
       - `"dependencies": { "@protostar/artifacts": "workspace:*", "zod": "^3.25.76" }`,
       - `"devDependencies": { "typescript": "^5.6.0", "@types/node": "^22" }` (match versions used by `@protostar/intent`),
       - `"protostar": { "tier": "pure" }`.
    2. Create `packages/replay/tsconfig.json` extending `../../tsconfig.base.json` with `outDir: "./dist"`, `rootDir: "."`, references to `@protostar/artifacts`.
    3. Create `packages/replay/AGENTS.md` (≤15 lines) declaring tier=pure, no fs/net imports, zod-based schemas only.
    4. Create `packages/replay/README.md` following `.planning/templates/package-readme.md` shape (mirror an existing pure package README).
    5. Append `packages/replay` to `pnpm-workspace.yaml` `packages:` list.
    6. Add `@protostar/replay` and `@protostar/replay/*` path aliases to `tsconfig.base.json` `compilerOptions.paths` (mirror `@protostar/intent` entries).
    7. Implement `src/edit-set/schema.ts` exactly per 13-RESEARCH.md "EditSet Schema (REPLAY-01)" code block (zod with the path regex `/^piles\/(planning|review|execution-coordination)\/iter-\d+\/(result|trace)\.json$/`).
    8. Implement `src/edit-set/parse-address.ts` with `parseEditAddress(input: string): { path: string; jsonPointer?: string }`. Split on first `#`. Refuse if input has 2+ `#`. Refuse empty path. Re-validate path against the same regex from schema.ts.
    9. Implement `src/edit-set/allowlist.ts` exporting `isPileOutputPath(path: string): boolean` using the same regex.
    10. Implement `src/edit-set/hash.ts`: `computeEditSetHash(set: EditSet): string` using `sortJsonValue` + `createHash("sha256")` + `.digest("hex")`.
    11. Implement `src/json-pointer.ts` per RFC 6901: `parsePointer`, `resolvePointer`, `setPointer`. ~30-50 LOC total.
    12. Tests for each module (RED → GREEN). Place tests next to source as `*.test.ts`.
    13. Run `pnpm install` from repo root, then `pnpm --filter @protostar/replay build && pnpm --filter @protostar/replay test`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; pnpm install &amp;&amp; pnpm --filter @protostar/replay build &amp;&amp; pnpm --filter @protostar/replay test &amp;&amp; grep -q '"protostar.tier": "pure"' packages/replay/package.json &amp;&amp; grep -q '"protostar"' packages/replay/package.json &amp;&amp; grep -qE '"sideEffects":\s*false' packages/replay/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @protostar/replay test` exits 0
    - `grep -q '"tier": "pure"' packages/replay/package.json` succeeds
    - `grep -q '@protostar/replay' tsconfig.base.json` succeeds (path alias added)
    - `grep -q 'packages/replay' pnpm-workspace.yaml` succeeds
    - `grep -c '\.test\.ts$' packages/replay/src -r --include='*.test.ts' | wc -l` shows ≥6 test files (schema, parse-address, allowlist, hash, json-pointer, plus task-2 schemas)
    - EditSchema regex matches the literal `^piles\/(planning|review|execution-coordination)\/iter-\d+\/(result|trace)\.json$`: `grep -q 'piles\\\\/(planning|review|execution-coordination)' packages/replay/src/edit-set/schema.ts`
  </acceptance_criteria>
  <done>Workspace builds, tests pass, all schemas/parser/allowlist/JSON Pointer compile and have RED→GREEN coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PathsReadRecord, OverlayManifest, CounterfactualReport schemas + schema registry + tier contracts</name>
  <files>
    packages/replay/src/paths-read-record.ts,
    packages/replay/src/paths-read-record.test.ts,
    packages/replay/src/overlay-manifest/schema.ts,
    packages/replay/src/overlay-manifest/schema.test.ts,
    packages/replay/src/counterfactual/schema.ts,
    packages/replay/src/counterfactual/schema.test.ts,
    packages/replay/src/schema-registry/index.ts,
    packages/replay/src/schema-registry/index.test.ts,
    packages/replay/src/no-fs.contract.test.ts,
    packages/replay/src/no-net.contract.test.ts,
    packages/replay/src/index.ts,
    packages/admission-e2e/src/contracts/tier-conformance.contract.test.ts
  </files>
  <read_first>
    - .planning/phases/13-replay-with-edit-run-bundles/13-RESEARCH.md (Code Examples — Overlay Manifest, PathsRead Sidecar, Counterfactual Report blocks; copy literals)
    - packages/intent/src/no-fs.contract.test.ts OR packages/lmstudio-adapter/src/no-fs.contract.test.ts (precedent for no-fs / no-net contract tests — recursive grep over src/, refuses node:fs imports)
    - packages/authority/src/no-net.contract.test.ts (or similar — precedent for no-net contract)
    - packages/admission-e2e/src/contracts/tier-conformance.contract.test.ts (existing entries; pattern for adding a new package row)
  </read_first>
  <behavior>
    - PathsReadRecordSchema parses the example record from 13-RESEARCH.md (stage="execution", pathsRead with sha256 hex, inputsHash, outputsHash); rejects non-hex hashes, rejects `stage` not in the enum.
    - OverlayManifestSchema parses entries with `originalSha256` 64-hex and nonneg int `originalBytes`; rejects bad hash, negative bytes.
    - CounterfactualReportSchema parses the verdict-changed example; rejects `score` non-numeric; `evolutionSignal.confidence` must be in [0,1].
    - SchemaRegistry: `getSchemaForPath("piles/review/iter-3/result.json")` → returns the pile-result zod schema (a generic z.record passthrough is acceptable in v1 — D-04 schema validation re-uses the *same schema the original run wrote*; v1 minimum is a permissive "valid JSON object" schema for pile result/trace, with a TODO marker for stricter shape in v2). `getSchemaForPath("manifest.json")` → returns undefined (only pile-output paths registered in v1).
    - no-fs.contract.test.ts: globs `src/**/*.ts`, asserts no `from "node:fs"` / `node:fs/promises` / `from "fs"` imports. (Uses node:fs ONLY in the test file itself, which lives outside the source production rule by mirroring intent's pattern.)
    - no-net.contract.test.ts: same but for `node:http`, `node:https`, `node:net`, `node:dgram`, and `fetch(`.
  </behavior>
  <action>
    1. Implement `paths-read-record.ts` with `PathsReadRecordSchema` exactly per 13-RESEARCH.md Code Examples block (note: the research example is missing the `inputsHash` field in one place — include it; it appears in the type list immediately after `pathsRead`).
    2. Implement `overlay-manifest/schema.ts` per 13-RESEARCH.md.
    3. Implement `counterfactual/schema.ts` per 13-RESEARCH.md.
    4. Implement `schema-registry/index.ts` exporting `getSchemaForPath(path: string): z.ZodTypeAny | undefined`. v1 returns a generic `z.object({}).passthrough()` for pile-output paths matching the allowlist regex; undefined otherwise. Document the v2 expansion in a code comment.
    5. Implement `src/no-fs.contract.test.ts` and `src/no-net.contract.test.ts` mirroring an existing pure package's contract tests. Both use `node:test` + `node:fs` (in test only) to recursively glob `packages/replay/src/**/*.ts` minus `*.test.ts` and assert no forbidden imports.
    6. Update `src/index.ts` to barrel-export every public symbol (EditSet types, schemas, parser, allowlist, hash, JSON Pointer helpers, PathsReadRecord, OverlayManifest, CounterfactualReport, schema registry).
    7. Add a row for `@protostar/replay` to `packages/admission-e2e/src/contracts/tier-conformance.contract.test.ts` matching the pattern used for other pure packages: assert `protostar.tier === "pure"`, `sideEffects === false`, presence of `no-fs.contract.test.ts` and `no-net.contract.test.ts`. Also add `@protostar/replay` to `packages/admission-e2e/package.json` deps if it is not already covered by the recursive glob.
    8. `pnpm --filter @protostar/replay build && pnpm --filter @protostar/replay test`.
    9. `pnpm --filter @protostar/admission-e2e build && pnpm --filter @protostar/admission-e2e test` to verify tier-conformance includes the new package.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/protostar &amp;&amp; pnpm --filter @protostar/replay build &amp;&amp; pnpm --filter @protostar/replay test &amp;&amp; pnpm --filter @protostar/admission-e2e build &amp;&amp; pnpm --filter @protostar/admission-e2e test &amp;&amp; grep -q 'PathsReadRecordSchema' packages/replay/src/index.ts &amp;&amp; grep -q 'OverlayManifestSchema' packages/replay/src/index.ts &amp;&amp; grep -q 'CounterfactualReportSchema' packages/replay/src/index.ts &amp;&amp; grep -q '@protostar/replay' packages/admission-e2e/src/contracts/tier-conformance.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four schemas exported via `packages/replay/src/index.ts`: `grep -c 'export.*\(EditSet\|PathsReadRecord\|OverlayManifest\|CounterfactualReport\)' packages/replay/src/index.ts` ≥ 4
    - `pnpm --filter @protostar/replay test` passes (RED→GREEN evidence in commit history per task)
    - `packages/replay/src/no-fs.contract.test.ts` and `packages/replay/src/no-net.contract.test.ts` both exist and pass
    - `packages/admission-e2e/src/contracts/tier-conformance.contract.test.ts` mentions `@protostar/replay`
    - Top-level `pnpm run verify` still green
  </acceptance_criteria>
  <done>Phase 13 schemas and tier conformance pinned. Downstream waves consume `@protostar/replay` types directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied edit file → EditSet schema | untrusted JSON crosses zod parser at replay-CLI entry (V5) |
| operator-supplied edit address → JSON Pointer | path-traversal vector if pointer escapes target document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | edit address path containing `..` or `/` escapes | mitigate | Allowlist regex `^piles\/(planning|review|execution-coordination)\/iter-\d+\/(result|trace)\.json$` rejects traversal at parse-address.ts and again at allowlist.ts |
| T-13-02-02 | Tampering | malformed JSON Pointer breaks hand-rolled parser | mitigate | RFC 6901 grammar enforced in `json-pointer.ts` parse step; refuses bare `~` not followed by `0`/`1`; refuses non-leading `#` |
| T-13-02-03 | Tampering | hash collision in editSetHash | accept | sha256 collision-resistance is project-wide assumption (Phase 2 Q-15 lock) |
| T-13-02-04 | Information Disclosure | replay package accidentally imports node:fs | mitigate | `no-fs.contract.test.ts` enforces import discipline |
| T-13-02-05 | Information Disclosure | replay package imports network primitives | mitigate | `no-net.contract.test.ts` enforces import discipline |
| T-13-02-06 | Spoofing | EditSet without parentRunId verification | mitigate | `EditSetSchema.parentRunId` is required; downstream plan 13-07 cross-checks against the bundle path |
</threat_model>

<verification>
- `pnpm --filter @protostar/replay test` green
- `pnpm --filter @protostar/admission-e2e test` green (tier-conformance accepts new package)
- `pnpm run verify` green at top level
- `grep -r 'from "node:fs' packages/replay/src/ --include='*.ts' | grep -v '\.test\.ts'` empty (no production fs imports)
</verification>

<success_criteria>
- @protostar/replay workspace builds and tests pass with zero fs/net production imports
- All 4 schemas (EditSet, PathsReadRecord, OverlayManifest, CounterfactualReport) export validated zod parsers
- Tier-conformance contract recognizes the new pure-tier package
- REPLAY-01..04 are implementable end-to-end against the schemas exported here
</success_criteria>

<output>
After completion, create `.planning/phases/13-replay-with-edit-run-bundles/13-02-SUMMARY.md`
</output>
